# 阶段一：协议与执行路径基线

日期：2026-07-08

## 1. 范围

本阶段只固定现有远程控制主链路的行为，并补第一层安全护栏，不改变业务语义。

已覆盖：

- `common`：长度帧、初始化帧、协议预留握手、基础序列化测试。
- `ctrl_common`：控制端业务帧与响应解析的畸形输入保护。
- `ctrl_server`：四类连接的入口分派和按角色收紧帧上限。
- `real_ctrl`：控制端连接、数据连接、本地 pipe API 入口和消息大小限制。
- `ctrl_kik`：被控端控制连接和数据连接的帧上限接入。

明确不覆盖：

- `screen_stream` 暂不改动。
- Android 控制端暂不实现，只保留未来复用 `real_ctrl` 安全模型的协议空间。

## 2. 当前执行拓扑

```text
real_ctrl
  ctrl_conn        -> 控制通道 -> ctrl_server
  ctrl_data_conn   -> 数据通道 -> ctrl_server

ctrl_server
  core::server::handle_stream
    -> LengthFieldBasedFrameDecoder
    -> ChannelType::Unknown
    -> handler::read_handle::handle_init_message
    -> ChannelType::Ctrl | CtrlData | Kik | KikData
    -> handler::read_handle::{handle_ctrl, handle_ctrl_data, handle_kik, handle_kik_data}

ctrl_kik
  kik_conn         -> 被控命令通道 -> ctrl_server
  kik_data_conn    -> 被控数据通道 -> ctrl_server
```

所有远程 TCP 连接当前仍是 `TcpStream + u32 big-endian length + body`。本阶段没有引入 TLS，因此还没有完成“数据传输层加密”和“防中间人攻击”；这些属于下一阶段 `real_ctrl -> ctrl_server` 强身份传输改造。

## 3. 通道角色与初始化流程

连接建立时服务端先创建 `ChannelType::Unknown`，第一帧必须是 `InitFrame`。服务端根据 `InitFrame` 将连接转为具体角色。

| 角色 | 首帧 | 服务端返回 | 当前语义 |
| --- | --- | --- | --- |
| `Ctrl` | `CtrlAuthReq(String)` | `CtrlAuthReply(bool)` | `real_ctrl` 控制通道。当前字符串是历史静态鉴权摘要，只能作为业务兼容，不是抗 MITM 安全边界。 |
| `CtrlData` | `CtrlDataConnReq(String)` | `CtrlDataConnAuthReply(bool)` | `real_ctrl` 数据通道。当前依赖历史摘要绑定，后续应改为认证 session + nonce。 |
| `Kik` | `KikReq(KikInfo)` | `KikId(String)` | `ctrl_kik` 被控命令通道。`id` 是实例标识，不是安全凭据。 |
| `KikData` | `KikDataConnReq(String)` | `KikDataConn(bool)` | `ctrl_kik` 被控数据通道。服务端后续需要校验其生命周期绑定到已在线 `Kik` 通道。 |

关键边界：

- `real_ctrl` 是强身份控制端，后续必须校验 `ctrl_server` 身份，防止中间人攻击。
- `ctrl_kik` 是极小被控客户端，不做账号式身份校验，不读取除服务端 IP/端口之外的服务端机器信息。
- 如果 `ctrl_kik` 不校验服务端身份，就不能声明它具备抗主动 MITM 能力。

## 4. 长度帧护栏

现有外层帧格式：

```text
u32 big-endian body_len
body bytes
```

本阶段新增的帧上限：

- `CONTROL_MAX_FRAME_LENGTH = 1 MiB`
- `DATA_MAX_FRAME_LENGTH = 1 GiB + 16 MiB`
- `DEFAULT_MAX_FRAME_LENGTH = DATA_MAX_FRAME_LENGTH`

接入方式：

- `real_ctrl::ctrl_conn` 使用控制通道上限。
- `real_ctrl::ctrl_data_conn` 使用数据通道上限。
- `ctrl_kik::kik_conn` 使用控制通道上限。
- `ctrl_kik::kik_data_conn` 使用数据通道上限。
- `ctrl_server` 在读取 `InitFrame` 前还不知道角色，因此先用数据通道上限保持兼容；首帧处理完成后按 `ChannelType` 收紧到控制或数据上限。

实现细节：

- `LengthFieldBasedFrameDecoder::new_with_max_frame_len` 允许不同通道配置不同上限。
- 超过上限直接返回 `InvalidData`，避免按恶意长度分配内存。
- `set_max_frame_len` 只在当前没有半包时生效，避免正在解析的半帧被中途换规则。

## 5. 协议预留握手

新增 `common::message::protocol_hello::ProtocolHello`，但当前没有接入真实连接流程。

它的目的不是替换现有 `InitFrame`，而是为后续安全能力迁移提供显式版本和能力协商点：

```text
magic        = "RTHL"
version      = 1
role         = Ctrl | CtrlData | Kik | KikData
capabilities = bit flags
```

已预留能力：

- `CAP_SERVER_IDENTITY_PIN`：控制端支持服务端身份 pin。
- `CAP_SESSION_BINDING`：数据通道支持绑定认证 session。
- `CAP_LIMITED_CTRL_KIK`：被控端按极小客户端模型运行。

后续原则：

- 新安全能力只在新协议能力协商成功后启用。
- 老协议可保留兼容窗口，但不能在 `real_ctrl` 强安全模式下自动降级到明文。

## 6. 畸形输入保护

本阶段修复了若干短帧/空帧可能导致 panic 的路径：

- `common::message::kik_frame::KikFrame`
- `ctrl_common::ctrl_frame::Frame`
- `ctrl_common::ctrl_resp::Resp`
- `ctrl_common::ctrl_resp::ServerResp`

解析原则：

- 空帧、短帧、长度不足应返回 `None` 或结构化错误，不允许 panic。
- 自定义二进制协议每新增字段都要同步补 round-trip 和畸形输入测试。

## 7. real_ctrl 本地开放 API 基线

新增 `real_ctrl::api_service::RealCtrlApi`，作为 CLI、命名管道、HTTP 未来统一复用的服务层入口。

当前状态：

- 命名管道服务端已通过 `RealCtrlApi::execute` 调用 `dispatch::distribution_other`。
- 命名管道请求上限：`MAX_PIPE_REQUEST_BYTES = 1 MiB`。
- 命名管道响应上限：`MAX_PIPE_RESPONSE_BYTES = 64 MiB`。
- pipe 客户端和服务端都校验长度，避免按对端声明的恶意长度直接分配内存。

仍待后续完成：

- HTTP handler 还需要逐步改为直接复用 `RealCtrlApi`，而不是继续散落命令拼装逻辑。
- pipe 协议仍是历史 `u32 len + postcard(InputCommand)`，后续需要升级为带 `version/request_id/deadline/method/body` 的 envelope。
- Windows 命名管道还需要补 ACL/SDDL，只允许当前用户和管理员访问。

## 8. 阶段一验收标准

本阶段完成后应满足：

- 不改动 `screen_stream`。
- `real_ctrl`、`ctrl_kik`、`ctrl_server` 仍能按旧业务语义编译。
- 控制通道和数据通道有不同帧上限。
- 服务端在识别连接角色后会收紧对应连接的帧上限。
- 短帧/空帧解析不 panic。
- `ProtocolHello` 有独立 round-trip 与非法输入测试，但不改变现有上线流程。
- `real_ctrl` 本地 pipe API 有统一服务入口和请求/响应大小护栏。

建议验证命令：

```powershell
cargo test -p common -p ctrl_common
cargo check -p real_ctrl -p ctrl_kik -p ctrl_server
git diff -- screen_stream
```

## 9. 下一阶段入口

下一阶段建议从 `real_ctrl -> ctrl_server` 强身份传输开始：

1. 在 `common` 抽象 transport connector/acceptor。
2. 为 `ctrl_server` 增加 `real_ctrl` 专用 TLS 管理端口。
3. 使用 `rustls`/`tokio-rustls` 实现 TLS 1.3。
4. `real_ctrl` 校验服务端 SPKI pin 或私有 CA，不绑定 IP。
5. 禁止 `real_ctrl` 强安全模式降级到明文。
6. `ctrl_kik` 默认不引入这套身份校验链路，继续保持极小客户端边界。

## 生产收敛补充（2026-07-11）

- 握手阶段帧上限从历史数据上限收紧为 4 KiB，只有角色认证完成后才切换到 1 MiB 控制上限或历史数据上限。
- `KikInfo`、`Dok`、correlation id、data id、HMAC 字段新增精确长度和短帧检查；空帧、尾随数据、范围与数据长度不一致均返回 `None`。
- 删除已完全注释且无调用方的 `common::async_util`，数据路径统一使用 Tokio 有界队列和 TCP 背压。
- build.rs 生成代码迁移到 Cargo `OUT_DIR`，普通构建不再修改 `src/generated`。
