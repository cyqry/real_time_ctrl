# ctrl_kik 截屏实现与验收

## 最终接口

调用者可以明确选择两个函数：

```rust
// 历史 GDI 实现，函数体保持不变。
let png = screen::cut_screen().await?;

// 推荐生产实现，三种档位均为逐像素无损 PNG。
let png = screen::cut_screen_dxgi(screen::PngProfile::Balanced).await?;
```

`ctrl_kik::cmd_runner` 的 `CtrlCommand::Screen` 默认使用 `Balanced`。需要最低延迟时选 `Fast`；只有带宽或归档体积优先时才选 `High`。

## 开闭原则扩展模型

命令执行层只创建 `CaptureRequest` 并调用 `capture_screen()`，不依赖 DXGI 或 GDI。默认组合根按顺序注册：

```text
ScreenCaptureService
  -> DxgiDesktopDuplicationBackend
  -> LegacyGdiCaptureBackend
```

每个后端只返回统一的 `CaptureArtifact`：

- `Accept`：当前结果可以直接返回，管线停止。
- `VerifyWithFallback`：当前结果需要复核，管线继续下一个后端。
- `Err`：记录后端名和错误，继续下一个后端。

新增捕获实现不需要修改服务调度器或 `cmd_runner`，只需实现接口并在组合根注册：

```rust
use crate::screen::backend::{
    CaptureArtifact, CaptureFuture, CaptureRequest, ScreenCaptureBackend,
};

struct WindowsGraphicsCaptureBackend;

impl ScreenCaptureBackend for WindowsGraphicsCaptureBackend {
    fn name(&self) -> &'static str {
        "windows-graphics-capture"
    }

    fn capture(&self, request: CaptureRequest) -> CaptureFuture<'_> {
        Box::pin(async move {
            let png = capture_with_wgc(request.png_profile()).await?;
            Ok(CaptureArtifact::accepted_png(png))
        })
    }
}
```

只有 `default_capture_service()` 负责选择生产后端及顺序，这是允许依赖具体实现的唯一组合位置。

截屏代码全部位于 `ctrl_kik/src/screen/`：

- `mod.rs`：公开接口和默认后端组合根。
- `backend.rs`：稳定扩展契约与管线调度器。
- `desktop_duplication.rs`：D3D11/DXGI 采集、像素转换和 PNG 编码。
- `dxgi_backend.rs`：专用线程、请求队列、会话生命周期和 DXGI 后端适配。
- `legacy_gdi.rs`：原 GDI 函数实现；`gdi_backend.rs` 只做统一接口适配。
- `tests.rs`：需要交互桌面的效果和性能对比测试。

## 黑屏根因

现场复现中，DXGI duplication 会话的第 0 帧是 2560x1440 全零资源，PNG 只有 13969 字节；同一会话的第 1 帧开始恢复完整桌面。像素转换和 PNG 编码本身没有错误。

新实现只在会话首帧检测全零并重取一次，稳定热路径不增加全帧扫描。若驱动连续返回全黑，新入口使用旧 GDI 路径复核，因为纯黑也可能是合法桌面，不能直接当成错误。

## 新执行路径

1. `capture_screen()` 把请求交给稳定的 `ScreenCaptureService` 管线。
2. DXGI 后端把请求写入容量为 1 的有界队列，排队和处理都有超时。
3. 专用 Windows 线程持有 `PrimaryScreenCapturer`，保证 `D3D11_CREATE_DEVICE_SINGLETHREADED` 的线程约束。
4. DXGI 获取桌面纹理，复制到可读 staging texture，再按驱动提供的 `RowPitch` 映射。
5. BGRA 转 RGB 时处理主屏旋转，复用 RGB、staging texture 和 D3D11/DXGI 会话。
6. `png` crate 直接编码 RGB8；不保留无意义 alpha，三种压缩档位均不改变像素。
7. 瞬时设备错误会重建 duplication 一次；其他错误或待复核帧交由管线执行后续后端。

首次成功后，工作线程把帧等待收紧到 100 ms。静止桌面可直接复用最近完整帧，避免默认 750 ms 等待拖慢命令链路。

DXGI Session 在最后一次请求处理完成后只空闲保留 30 秒。到期后，工作线程在原线程内释放 D3D11 device/context、duplication、staging texture 和 RGB 缓冲；下一次请求重新枚举当前主屏并惰性初始化。标准库工作线程与容量为 1 的请求队列保持常驻，它们不持有桌面帧或显卡会话，避免每次请求重复创建线程，同时满足状态不长期常驻。

## 旧实现风险

根据“原函数不改动”的要求，`cut_screen()` 函数体保留，但不再作为生产默认：

- 24bpp `GetDIBits` 的行跨度要求 4 字节对齐，原实现按 `width * 3` 分配和索引，宽度不满足对齐时存在错误风险。
- 没有检查 `BitBlt`、`GetDIBits`、兼容 DC/bitmap 和 `SelectObject` 的返回值。
- 成功路径末尾才释放 GDI 对象，中途错误可能泄漏句柄。
- 截屏和最高压缩同步运行在异步调用线程上，CPU 开销较高。
- 现场曾观察到旧路径受进程 DPI awareness 影响输出 2048x1152，而 DXGI 始终返回主屏物理分辨率 2560x1440。

## Release 现场结果

2026-07-11 的一轮同桌面测试如下。桌面内容会影响 PNG 体积，数据只用于本机实现选型：

| 路径/档位 | 耗时 | PNG 大小 | 说明 |
| --- | ---: | ---: | --- |
| 旧 GDI Best | 615.5 ms | 381947 B | 历史实现 |
| DXGI Balanced 首次捕获+编码 | 226.2 ms | 384085 B | 推荐默认 |
| DXGI Fast 仅编码 | 17.4 ms | 896429 B | 最低延迟 |
| DXGI Balanced 仅编码 | 218.9 ms | 384085 B | 体积/CPU 平衡 |
| DXGI High 仅编码 | 383.9 ms | 381948 B | 本帧只比 Balanced 小约 0.6% |
| DXGI worker Fast 首次/稳定 | 58.7 / 46.2 ms | 896573 / 878073 B | 验证会话复用 |

Fast、Balanced、High 使用同一份 RGB 原始帧编码，测试会逐字节比较解码后的 RGB，已确认完全一致。

目录化和 30 秒 Session 过期改造后的回归结果：DXGI Balanced 为 2560x1440、390648 B，首次采集加编码 241.5 ms；worker Fast 首次 77.4 ms、稳定请求 24.3 ms。旧 GDI 同帧为 409736 B、695.0 ms。桌面内容不同会改变体积和耗时，本轮重点是确认画面完整、非黑屏、三档像素一致且热路径复用正常。

## 验收与限制

手工桌面验收命令：

```powershell
cargo test -p ctrl_kik --release screen::tests::compare_capture_backends -- --ignored --nocapture
```

交互测试的临时产物位于 `target/screen`；确认需要长期留作维护基线的 PNG 与
`comparison.json` 复制到 `reports/screen`。测试依赖 Windows 交互桌面，因此标记为 `ignore`，
不会破坏无桌面的常规测试。当前 `screen` 模块冻结，所以本轮只迁移既有维护报告，不修改测试实现。

当前范围只捕获主屏，不合成硬件鼠标指针；Windows 安全桌面、锁屏和受保护视频内容仍可能被系统禁止捕获。`screen_stream` 未修改。
