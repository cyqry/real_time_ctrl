# real_time_ctrl 协议说明

本文描述当前生产协议包含的角色、消息和执行语义，供开发、联调、安全审计和版本发布使用。它刻意不记录
操作码、字段偏移、整数宽度、密钥、公钥或证书内容；精确编码以代码和测试为准。

当前远程线协议版本为 v4，不承诺向后兼容。`real_ctrl`、`ctrl_server`、`ctrl_kik` 必须使用同一版本整体发布，
未知版本、未知消息、畸形长度或多余字段均应拒绝，不能降级到旧明文或旧认证协议。本地开放 API 单独使用
自己的版本号，避免远程线协议和用户 API 被迫同步演进。

## 1. 参与组件与信任边界

| 组件 | 职责 | 持有的信息 |
| --- | --- | --- |
| `real_ctrl` | 发起控制命令，提供控制台、HTTP 和命名管道入口 | 控制账号、实例标识、TLS 校验材料、控制认证秘密 |
| `ctrl_server` | 认证控制端，维护会话和 ACL，选择 Kik，改写关联 ID，转发控制与数据 | TLS/Noise 服务端身份、账号策略、在线状态和临时路由 |
| `ctrl_kik` | 执行被控命令和文件 I/O | 服务端地址、Kik 端口、固定 Noise 服务端公钥、自身路由 ID |

`ctrl_kik` 的 ID 和名称用于路由与展示，不是账号凭据，也不能作为强设备身份。真正的账号认证、授权、
并发配额和会话隔离由 `real_ctrl` 与 `ctrl_server` 的控制面承担。

## 2. 连接和传输层

生产环境将控制端和 Kik 放在两个独立监听端口，服务端不再按首包内容在同一端口分流：

| 连接角色 | 发起方 | 传输保护 | 承载内容 |
| --- | --- | --- | --- |
| Ctrl | `real_ctrl` | pinned TLS 1.3 | 控制端认证、命令、响应、心跳 |
| CtrlData | `real_ctrl` | pinned TLS 1.3 | 绑定 Ctrl 会话的文件和截图数据、完成确认 |
| Kik | `ctrl_kik` | Noise NK | Kik 注册、命令、响应、心跳 |
| KikData | `ctrl_kik` | Noise NK | 绑定 Kik 的文件和截图数据 |

灰度端口为 Kik `9005`、Ctrl `9009`；正式端口为 Kik `9002`、Ctrl `9007`。相同端口配置必须使服务端
启动失败。

Ctrl 侧先完成 TLS 1.3 握手，并同时校验证书链、服务名和叶子公钥 pin；TLS 后再用部署控制秘密完成
账号/实例挑战应答。Kik 侧每条连接独立完成 Noise NK 握手，只验证服务端是否持有固定 X25519 私钥，
不读取证书、服务名、账号或机器信息。两侧均没有明文降级路径。

Ctrl 连接会在 TLS ClientHello 前发送一个不含秘密的 v4 连接前导，用于兼容会重置非标准端口首包 TLS 的
公网中间设备。该前导不参与身份认证、授权或加密协商，也不是降级信号；服务端收到后仍必须完成完整 TLS
校验。Ctrl 与 CtrlData 共用 TLS 监听端口并在加密后的初始化阶段确定角色；Kik 与 KikData 以相同方式共用
Noise 监听端口。TLS 端口与 Noise 端口始终分离。

应用消息使用有长度边界的帧。握手阶段只允许较小初始化帧，角色认证成功后才切换为控制帧或数据帧上限。
当前基线约为：控制帧 1 MiB、数据帧 64 MiB；Noise 记录层会自动把较大的应用帧拆成多个加密记录，
上层不感知记录边界。连接通过 Ping/Pong 保活，读写停滞、认证失败、AEAD 校验失败或非法帧都会关闭连接。

## 3. 初始化消息

### 3.1 Ctrl 主连接

Ctrl 主连接的初始化依次包含：

1. 账号 ID、控制实例 ID 和客户端随机数；
2. 服务端随机挑战；
3. 客户端证明，绑定账号、实例及双方随机数；
4. 服务端签发的临时会话 ID。

同账号的不同实例拥有不同会话；同账号同实例重连只替换自身旧会话，不得挤掉同账号其他实例或其他账号。
会话 ID 只用于绑定后续数据连接，不能替代 TLS 身份校验或账号证明。

### 3.2 CtrlData 数据连接

每条 CtrlData 连接提交 Ctrl 会话 ID、新的通道随机数和通道证明，服务端返回接受或拒绝。通道证明绑定
当前会话和本条连接；重复随机数会被拒绝，以防止捕获后的握手重放。一个 Ctrl 会话可建立多条数据连接，
至少一条成功时允许降级运行。

### 3.3 Kik 主连接

Kik 主连接提交显示名称和可选的既有 Kik ID，服务端返回本次使用的 Kik ID。首次连接由服务端分配 ID；
重连可以继续携带既有 ID。服务端据此登记在线状态和最近上下线时间，但该 ID 本身不构成强身份认证。

每个控制会话尚未显式选择目标时，服务端自动选择该账号 ACL 允许、最近上线的在线 Kik。显式 `sys_use`
优先；当前 Kik 完整下线后，服务端才切换到仍在线的合法候选。

### 3.4 KikData 数据连接

每条 KikData 连接在独立 Noise 握手后提交 Kik ID，服务端确认该 Kik 当前在线且未超过数据连接配额，
随后把连接加入该 Kik 的数据连接池。连接池中的任意健康连接都可承载任意数据分片。

## 4. 控制消息模型

角色初始化完成后的活动帧类别如下：

| 通道 | 活动帧 |
| --- | --- |
| Ctrl | 命令、响应、Ping、Pong |
| CtrlData | 数据、下载完成确认 `DataAck` |
| Kik | 命令、带命令关联 ID 的响应、Ping、Pong |
| KikData | 数据 |

心跳不携带业务信息。数据帧不能代替控制命令，`DataAck` 只表示控制端已经结束本次下载数据路由，
不代表文件内容在业务上成功，也不会转发为 Kik 命令。

每个远程命令都封装为 `ReqCmd`，包含：

- 命令关联 ID：把响应准确送回原请求；
- 命令选项：当前主要表示普通任务或长任务超时档位；
- 命令正文：系统命令、被控命令或 Exec。

普通档位适合交互命令，长任务档位适合大文件，长任务也有约四小时的有限截止时间，并非永久等待。
服务端向 Kik 转发前会生成新的内部命令 ID；Kik 只看到完成当前命令所需的最少信息，控制端原始关联 ID
不会直接成为 Kik 侧路由依据。

### 4.1 系统命令

| 命令 | 包含的参数 | 返回内容 |
| --- | --- | --- |
| `sys_list` | 无 | 当前账号 ACL 可见的在线 Kik 列表 |
| `sys_use` | Kik ID | 当前控制会话显式选择目标的结果 |
| `sys_now` | 无 | 当前目标、未选择或目标已离线状态 |
| `sys_history` | 可选 Kik ID | 服务端本进程观察到的有界最近上线/下线记录 |

`sys_history` 不是持久化审计库；服务端重启后不会伪装成仍保有历史。当前目标属于控制会话私有状态，
不同账号、不同实例不会互相覆盖。

### 4.2 被控命令

| 命令 | 线上包含的内容 | 说明 |
| --- | --- | --- |
| `ls` | Kik 端目录路径 | 返回目录项列表 |
| `get_file` | Kik 源路径、数据关联 ID | 下载小文件 |
| `get_big_file` | Kik 源路径、数据关联 ID | 分片下载大文件 |
| `set_file` | 数据关联 ID、Kik 目标路径 | 上传小文件 |
| `set_big_file` | 数据关联 ID、总长度、SHA-256、Kik 目标路径 | 分片上传大文件 |
| `screen` | 数据关联 ID | 截图通过数据连接返回 |
| `exec` | 要执行的命令文本 | Kik 默认具备执行能力；管理面仍需两层策略允许 |

控制端本地源路径、本地保存路径和 HTTP 响应形式不发送给 Kik。`local_exit` 也只属于本地控制台生命周期，
不是线上命令。

### 4.3 响应

Ctrl 响应带原控制会话可识别的命令关联 ID，响应来源分为服务端和 Kik：

- 服务端响应：信息类成功，或结构化错误码与错误消息；
- Kik 成功响应：文本信息、小数据关联 ID，或大文件的数据 ID、总长度和 SHA-256；
- Kik 失败响应：远端错误码与错误消息。

大文件下载元数据只走控制响应，不在数据连接上另发元数据首帧。这保证轻量元数据与命令因果关系一致，
同时允许大文件分片从任意数据连接乱序到达。

## 5. 数据消息与文件传输

数据帧包含数据关联 ID 和负载。大文件负载分为：

- `FilePart`：起始位置、结束位置和该范围的数据；
- `Err`：发送端读失败或接收端写失败。

当前发送分片通常为 4 MiB，最多保留 3 个在途分片，并在多条数据连接间轮询发送。接收端不依赖到达顺序，
而是按每个分片携带的地址范围写入；允许相同范围的完整幂等重试，但拒绝部分重叠、越界、范围与数据长度
不一致以及过量分片。大文件完成后必须校验总长度和 SHA-256。

### 5.1 小文件上传

1. `real_ctrl` 读取受上限约束的小文件并生成数据 ID；
2. `set_file` 的数据 ID 和 Kik 目标路径通过 Ctrl 主连接发送；
3. 原始文件内容通过任意 CtrlData 连接发送；
4. 服务端把外部数据 ID 改写为随机内部 ID，再通过任意 KikData 连接转发；
5. Kik 写入目标文件并通过控制响应报告结果。

### 5.2 小文件下载和截图

1. 控制命令只携带 Kik 路径或截图动作以及内部数据 ID；
2. Kik 通过任意 KikData 连接发送原始数据，并在控制响应中返回数据 ID；
3. 服务端恢复为当前 Ctrl 会话的数据 ID，通过任意 CtrlData 连接转发；
4. `real_ctrl` 消费数据后发送 `DataAck`，使服务端尽早释放下载路由。

数据帧可能先于控制响应到达。三端都必须使用有界的预到达缓存/短等待处理该乱序，不能把正常跨连接乱序
误判为“数据关联 ID 不匹配”，也不能永久保存迟到数据。

### 5.3 大文件上传和下载

大文件上传先在控制面发送数据 ID、总长度、SHA-256 和 Kik 目标路径，再在数据面发送 `FilePart`。大文件
下载先由 Kik 在控制响应中返回数据 ID、总长度和 SHA-256，随后数据面只发送 `FilePart` 或 `Err`。

接收端先在系统临时目录创建 CSPRNG 随机文件名的 `.temp` 文件并预分配空间，乱序写完且校验通过后再提交到
目标路径。失败、超时、会话断开和校验失败都必须清理临时文件与路由。当前大文件协议上限为 1 GiB；小文件
模式上限为 32 MiB，超过后应使用大文件命令。

服务端对每个数据帧完整解析并重新编码，以检查 ID、范围、长度和 UTF-8 边界；下游连接失败时可把完整帧
改投其他健康连接。转发任务保留背压，不创建无界发送任务。

## 6. 关联 ID 与并发隔离

协议中存在多层用途不同的标识，不能混用：

| 标识 | 作用域 | 用途 |
| --- | --- | --- |
| API `request_id` | 单个 HTTP/管道请求 | 原样回显，方便调用者匹配响应 |
| Ctrl 命令 ID | 单个 Ctrl 会话 | 路由并发命令响应 |
| Kik 内部命令 ID | 服务端到指定 Kik | 隐藏控制端关联信息并隔离转发 |
| 外部数据 ID | 单个 Ctrl 会话 | 控制端本地数据队列路由 |
| 内部数据 ID | 指定会话、Kik 和方向 | 服务端跨多条数据连接安全转发 |
| 会话 ID | 单个已认证 Ctrl 会话 | 绑定 CtrlData 连接和会话资源 |

映射必须同时绑定控制会话、目标 Kik 和传输方向。只比较裸数据 ID 会产生跨账号串流风险。会话断开、Kik
下线、命令完成、错误、超时或 `DataAck` 都会触发相应路由清理。

控制台逐条执行命令；HTTP 与命名管道允许独立请求并行和乱序完成。每个请求独占并发许可、响应 oneshot 和
数据 ID 队列。服务端再按全局、账号、实例和 Kik 四级配额限制并发，配额不足返回忙碌，不使用无界排队。
已写出但因连接故障丢失结果的命令不会自动重放，以免重复执行 `exec` 或文件写入。

## 7. 本地开放 API

本地 API 只负责把外部调用转换为同一 `RealCtrlApi` 服务层命令，不绕过远程认证、ACL、并发限制或关联路由。

### 7.1 HTTP

- `GET /api/health`：健康检查，不触发远程命令；
- `POST /api/v1/commands`：统一命令入口；
- `GET /api/screen`：受保护的截图便捷入口。

命令请求是 JSON，包含 API 版本、可选 `request_id` 和带 `kind` 的命令对象。支持的 `kind` 为
`sys_list`、`sys_now`、`sys_history`、`sys_use`、`ctrl_ls`、`ctrl_screen`、`ctrl_get_file`、
`ctrl_get_big_file`、`ctrl_set_file`、`ctrl_set_big_file` 和 `exec`。未知字段、空字符串、NUL、超长 ID、
超长路径及错误版本均拒绝。

响应包含版本、可选 `request_id`、成功标志，以及二选一的数据或错误。数据可以是信息、目录列表、Kik 列表、
当前选择、上下线历史或 Base64 二进制；错误类别覆盖参数错误、未认证、无权限、忙碌、未找到、远端错误、
响应过大、不支持版本和内部错误。大文件下载必须提供本地保存路径，禁止聚合进 HTTP 响应内存。

HTTP 默认只监听 loopback。命令和截图入口都要求 Bearer token 或专用 token 请求头；非 loopback 绑定还要求
足够强的 token，且必须由部署者显式配置。请求体和二进制响应均有上限。

### 7.2 Windows 命名管道

命名管道使用固定本地管道名、版本标识、长度边界和同一份 JSON `ApiRequest` / `ApiResponse` 契约。管道 DACL
限制可连接主体，每次读写有超时，连接数有上限。没有当前版本标识的旧请求直接拒绝，不尝试旧 postcard 协议。

## 8. 失败处理与版本演进

- 所有外部字符串、ID、路径、帧和文件都有长度或数量上限；
- 所有握手、读写、命令和文件传输都有有限超时；
- 空帧、短帧、长度不一致、未知消息、非法 UTF-8、错误角色和认证失败必须安全返回错误或断开，不能 panic；
- 日志不得记录控制秘密、token、私钥、完整证明或文件内容；
- 新增或修改消息字段时，必须同步修改本文件、三端实现、round-trip/畸形输入测试及端到端测试；
- 因当前远程协议不兼容旧版本，升级需要三端协调发布，不能添加静默兼容或明文降级分支。

精确实现入口：

- `common/src/message/init_frame.rs`：连接初始化；
- `common/src/protocol.rs`、`common/src/command.rs`：命令信封与命令；
- `common/src/message/kik_frame.rs`、`common/src/message/dok.rs`：Kik 帧和文件分片；
- `ctrl_common/src/ctrl_frame.rs`、`ctrl_common/src/ctrl_resp.rs`：Ctrl 帧和响应；
- `real_ctrl/src/api_contract.rs`：HTTP/命名管道稳定契约。
