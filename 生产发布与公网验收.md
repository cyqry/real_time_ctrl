# 生产发布与公网验收

## 一键入口

直接双击根目录 `publish_gray.cmd`。命令行等价入口：

```powershell
.\scripts\publish_stack.ps1 -Channel Gray
```

端口职责固定分离：

| 通道 | 灰度 | 正式 |
| --- | ---: | ---: |
| ctrl_kik Noise | 9005 | 9002 |
| real_ctrl pinned TLS | 9009 | 9007 |

本次仍只发布灰度；正式端口与目录仅在脚本中预置。

脚本按顺序完成以下工作：

1. 校验 `cargo-audit`、`cross`、Docker、OpenSSL 和 `sshtool`；Docker 未启动时立即停止并给出明确提示。发布前还会证明生产活动依赖树不含已知问题版本，并对其余 RustSec 项实行零容忍。
2. 在 `deploy/gray/identity/` 首次生成并后续复用 TLS 身份、Noise X25519 密钥、控制面高熵秘密和本地 API token。
3. 以固定 Rust 提交和重编译标准库构建、审计 `ctrl_kik`；灰度端点和 Noise 公钥只在构建期注入。
4. 以 `production` profile 构建 `real_ctrl`，保留 PDB；以 `cross` 构建 musl `ctrl_server`，保留 DWARF。
5. 校验服务端 SHA-256，原子替换远端灰度二进制，安装兼容目标机旧 systemd 的非 root 服务；只有两个职责端口都监听成功才提交部署，启动失败自动回滚。
6. 清空旧 artifact 目录，只复制可直接运行的 EXE、服务端 ELF、调试符号和审计回执，
   并扫描真实秘密、私钥和证书 Base64 是否意外以明文进入产物。
7. 从本机通过公网执行安全、功能、文件和性能验收，报告写入 `deploy/gray/reports/`。

双击入口就是 `deploy/gray/artifacts/` 中的 Windows EXE，不依赖 `.cmd`、`.ps1`、
证书或配置 sidecar。公开配置和每个进程必须持有的秘密都以混淆构建默认值写入，运行环境仍可
覆盖。混淆只能阻止直接静态搜索，无法替代 TPM、凭据管理器或服务端授权边界。

正式通道已经在脚本中预置独立目录和端口，但本次没有发布。只更新已有灰度产物和 unit 时可使用：

```powershell
.\scripts\publish_stack.ps1 -Channel Gray -DeployOnly
```

## 传输安全边界

`real_ctrl` 控制面使用独立公网端口上的 rustls TLS 1.3，并同时验证证书链、DNS 名称和固定 SPKI SHA-256。`ctrl_kik` 使用另一个独立公网端口上的 Noise NK；生产链路不再依靠应用层前导区分这两个角色。

公网实测发现，链路中的中间设备会复位非标准端口上“首包即 TLS”的连接。为保证可达性，`real_ctrl` 会先发送固定、非秘密的 `RTCT v4` 前导，再立即发送标准 TLS ClientHello；独立 TLS 监听器同时接受此前导路径和标准 ClientHello。此前导只用于穿透与协议识别，不携带凭据、不触发明文降级，也不替代证书链、DNS 名称和 SPKI pin 校验。

`ctrl_kik` 的命令连接与三条数据连接都使用 Noise NK（X25519、ChaCha20-Poly1305、BLAKE2s）。被控端不解析证书、服务名、SPKI 或服务器机器信息，只内置原始 X25519 公钥。严格来说，这个公钥用于验证对端持有对应私钥，是阻断主动中间人解密所不可省略的密码学下限；如果接受任意公钥，就只能防被动抓包，不能防主动中间人。

Noise 流适配器把上层帧切成最多 65535 字节的认证加密记录，每个方向使用独立、严格递增的 nonce。认证失败、截断、nonce 耗尽或半帧写失败都会关闭连接。上层仍保持控制帧 1 MiB、数据帧约 64 MiB和 4 MiB `FilePart`，没有恢复整文件聚合。

旧明文、静态摘要认证和同端口分流代码已经删除。Kik Noise 与 real_ctrl TLS 配成同一端口时服务端直接拒绝启动；v4 前导会明确拒绝旧客户端，三端必须整体发布。

## ctrl_kik 获得的最少远端信息

静态审计确认，被控端从远端只获得执行协议所需内容：

- Noise 握手不携带业务 payload；服务端公钥来自构建期，不从网络动态替换。
- 主连接只接收服务端分配的随机 Kik ID、心跳和当前控制命令。
- 数据连接只接收绑定成功布尔值、数据关联 ID 和实际文件/截图数据。
- 下载到控制端的本地保存路径不会发给被控端；`GetFile`、`GetBigFile` 的保留目标字段在上行前清空。
- 截图的控制端保存路径也在上行前清空，被控端只执行采集并返回数据。
- 大文件上传只携带数据 ID、总长度、SHA-256、被控端目标路径及带地址范围的分片，这些都是乱序落盘和完整性校验所必需的信息。

`ctrl_kik` 上报的显示名称属于上行信息，不属于本次“被控端获知远端信息”的审计口径，保持现有行为。

## 公网验收结果

灰度实例已完成以下验证：

- Kik Noise `9005` 与 real_ctrl TLS `9009` 均可从公网访问，且 Kik 端口会拒绝 real_ctrl TLS 角色。
- 错误 SPKI pin 被拒绝；正确 pinned TLS 控制连接成功。
- HTTP 无 token 请求被拒绝，本地 API 未暴露到公网。
- `ctrl_kik` 通过 Noise NK 接入；`sys_list`、`sys_history`（真实上线与退出后的下线时间）、`sys_use`、`sys_now`、`ctrl_ls` 和 Exec 双层授权通过。
- 小文件上传/下载摘要一致。
- 32 MiB 文件经多个 4 MiB 分片双向传输，最终 SHA-256 一致；复验使用 8 MiB 文件也通过。
- 远端运行文件 SHA-256 与本机构建产物一致，systemd 服务为 active 且以非 root 用户运行。
- 本地多实例 E2E 额外验证同账号两个实例、第二账号实例、12 个命名管道客户端和 12 条跨实例
  命令并行，账号选择、当前 Kik 与响应路由互不干扰；该测试与公网链路验收共同构成发布门禁。

2026-09-14 最终 32 MiB 公网验收中，上传 79.585 秒（0.40 MiB/s）、下载 80.685 秒
（0.40 MiB/s）；同轮 8 MiB SFTP 基线上行为 3.97 MiB/s、下行为 0.42 MiB/s，当前中继吞吐
达到慢方向基线的 95.2%。服务端在持有控制、数据连接和验收文件状态时为 60,492 KiB RSS、
5 个线程。多轮 32 MiB 复验结果一致。

结论是当前瓶颈为公网下行而非 CPU、内存、Noise 记录层或文件状态机。现有 3 条数据连接、3 个在途 4 MiB part 已提供约 12 MiB 的发送窗口，远大于当前带宽时延积；继续增加连接或窗口只会增加内存峰值和失败恢复成本。未来只有在更高带宽、高 RTT 网络实测不能跑满时，才应考虑自适应窗口，而不是固定扩大。

## 并发与渗透验收

测试范围和发布门禁见 `并发与渗透测试计划.md`，统一入口为：

```powershell
.\scripts\test_security_concurrency.ps1
```

最终本地门禁四阶段全部通过：运输 crate 测试、严格 Clippy、生产活动依赖审计，以及全栈安全并发
E2E。协议解析器另使用固定种子的 20,000 组随机短帧逐入口验证，畸形长度、短帧和尾随数据均未
触发 panic 或无界分配。

- 520 条停留在握手前的 Kik 连接不能耗尽 TLS 管理面许可；两个公网端口使用独立的 512 连接上限。
- 48 条约 3 秒命令同时到达时，16 条按实例配额并行完成，32 条在约 3.18 秒内明确拒绝；随后
  100 条稳定性请求全部成功，证明许可可回收且 `request_id` 没有串线。
- 12 个命名管道客户端在约 2.14 秒内并行完成；同账号多实例和多账号共 12 条命令在约 2.16 秒
  内完成。串行执行分别至少需要约 24 秒，因此结果证明服务端和本地 API 确实并行。
- 6 组文件并行往返及 12 MiB 多分片上传/下载的 SHA-256 全部一致；最新机器报告中的 12 MiB
  本地上传、下载分别约 2.47 秒和 2.48 秒，具体数值以每轮 `reports/e2e/e2e_report.json` 为准。
- 未授权畸形 JSON 在解析 body 前返回 401；未知字段、NUL、超限 body、错误方法、恶意 CORS、
  TLS 1.2、错误 pin、端口角色混淆、随机/截断握手均 fail-closed，攻击探针后健康检查成功。
- 公网 24 条突发命令中 16 条成功、8 条按配额拒绝，约 3.22 秒收敛；发布后服务无重启，warning
  级日志为 0。

机器可读结果位于 `reports/security-tests/security-concurrency-summary.json`、
`reports/e2e/e2e_report.json` 和 `deploy/gray/reports/public-e2e.json`。这些报告属于维护产物，
不会进入发布 artifact。

## 发布产物与已知边界

- `ctrl_kik` receipt 已通过全项目发布态源码明文、配置明文、源码路径、PDB、日志痕迹和 PE 缓解属性门禁；产物不包含应用名指纹。
- `ctrl_kik/src/screen` 的发布态字符串已纳入 `hidden!` 和全项目审计；最终结果以当次
  receipt 的 `full_project_plaintext_clean=true` 为准。
- `real_ctrl` 和 `ctrl_server` 按要求保留调试符号：前者生成 PDB，后者保留 DWARF，便于崩溃回溯和性能采样。
- `common` 已从停止维护的 `rustls-pemfile` 迁移到 rustls 官方 `rustls-pki-types`；生产活动依赖中的 `anyhow`、`crossbeam-epoch` 和 `h2` 已升级到修复版本。整仓 lockfile 仍记录冻结 `start` 的旧 h2/PEM 包及未启用的 rkyv 可选依赖，发布脚本会先逐 crate 证明它们不进入生产产物，再执行带理由的 RustSec 豁免。
- `target/` 只保存可重建的 Cargo 中间产物和测试工作文件，可随时整目录清理；身份、环境和发布
  artifact 位于 `deploy/`，本地验收报告位于 `reports/`。运行时文件接收临时文件按产品要求进入
  操作系统临时目录，采用随机名与 `.temp` 后缀。Cargo/Rust 依赖使用全局默认缓存。
