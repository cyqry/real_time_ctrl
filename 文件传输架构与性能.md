# 文件传输架构与性能

## 1. 核心拓扑与职责

控制命令固定经过：

`HTTP / 命名管道 / CLI -> RealCtrlApi -> ctrl_executor -> real_ctrl 控制连接 -> ctrl_server -> ctrl_kik 命令连接`

文件内容走独立数据面：

`发送端 3 路数据连接 -> ctrl_server 背压中继与逐帧换路 -> 接收端有界队列 -> 区间校验 -> 临时文件`

real_ctrl 一侧的控制/数据连接都使用 pinned TLS 1.3；ctrl_kik 一侧的控制/数据连接
都使用 Noise NK 认证加密。Noise 会把上层连续帧透明切分为约 64 KiB 的 AEAD 记录，
不改变分片地址、乱序落盘或重试语义。

轻量元数据不占用数据连接：

- 上传：`SetBigFile(data_id, total, sha256, remote_path)` 随控制命令发送。
- 下载：`ClientSuccessResp::BigFile(data_id, total, sha256)` 随控制响应返回。
- 数据面只出现 `Dok::FilePart(start, end, bytes)` 或 `Dok::Err`。

real_ctrl 的单命令门禁覆盖控制响应和关联数据处理全过程，因此同一会话只有一个消费者读取数据队列。多连接只并行同一轮命令的文件分片，不放开控制命令并发。

## 2. 小文件链路

### 上传到 ctrl_kik

1. real_ctrl 有界读取本地文件，最大 32 MiB。
2. 先写出 `SetFile(data_id, remote_path)` 控制帧；写成功后由本地 oneshot 门禁放行数据任务，没有额外网络往返。
3. 完整数据帧按轮询结果选择一条 CtrlData 连接；写失败时尝试本会话的其他连接。
4. ctrl_kik 按 `data_id` 取出内容，写入目标文件并 fsync。

### 从 ctrl_kik 下载

1. ctrl_kik 有界读取远端文件并发送单个数据帧。
2. real_ctrl 按 `data_id` 获取 `BytesMut`；保存到本地时直接写盘。
3. 开放 API 未提供 `local_path` 时可以返回二进制，但仍受 32 MiB 文件上限和 API 响应上限约束。

## 3. 大文件上传

1. real_ctrl 打开源文件一次，记录大小，用可复用 4 MiB 缓冲计算 SHA-256，再将同一句柄 seek 到开头生成分片流。
2. `SetBigFile(data_id, total, hash, remote_path)` 控制帧写成功后放行数据任务。
3. 发送窗口最多保留 3 个在途分片。每个 4 MiB `FilePart` 独立连续编码，并通过轮询分散到 3 条 CtrlData 连接；不同 TCP 流之间允许乱序。
4. ctrl_server 完整解析 CtrlData 帧，再编码为 KikData 帧，并从轮询位置开始尝试被控端数据连接。转发在当前读任务中等待，背压会一直传回发送端。
5. ctrl_kik 在操作系统临时目录以 CSPRNG 生成随机文件名并用 `create_new` 创建唯一 `.temp`，一次打开并 `set_len(total)`，按分片地址范围落盘。
6. 所有范围收齐后统一 flush、fsync，在同一打开句柄上重新计算 SHA-256，校验通过才提交到目标路径；同卷为原子移动，跨卷退化为 copy+fsync+remove。

## 4. 大文件下载

1. ctrl_kik 使用同一源文件句柄获取大小、计算 SHA-256、seek 回开头并准备 4 MiB 分片流。
2. 命令连接返回 `BigFile(data_id, total, hash)`；只有该控制响应成功写出后，才放行后台数据任务。
3. 最多 3 个在途 `FilePart` 轮询使用 KikData 连接。ctrl_server 对每帧解析、换协议编码，再轮询 CtrlData 连接。
4. real_ctrl 已从控制响应取得元数据，可从第一个到达的数据帧开始直接处理文件分片，不再等待 `FileMeta` 数据首帧。
5. 分片按地址范围写入系统临时目录的随机 `.temp` 文件；全部收齐并通过 SHA-256 后提交目标。同卷提交原子，跨卷复制提交期间目标路径不具备严格原子性。
6. `ctrl_get_big_file` 必须提供 `local_path`，禁止把大文件聚合为 Base64 HTTP/管道响应。

## 5. 乱序、重复与连接失败语义

`FilePart` 的闭区间 `[start, end]` 是落盘地址，不依赖接收顺序：

- 新区间：登记并写入对应偏移。
- 与已接收区间完全相同：视为连接重试产生的幂等副本，直接跳过。
- 部分重叠、越界、声明长度不符：立即按协议错误终止。
- 最终 SHA-256 不匹配：删除临时文件，绝不替换正式目标。

`Channel::write_and_flush` 遇到 I/O 错误或超时会把连接标记为关闭。超时可能取消一个已经写出部分字节的 `write_all`，该 TCP 流的帧边界不可再信任，因此后续选择必须跳过它。发送端和 ctrl_server 会用同一完整编码帧尝试下一条连接；若前一条连接实际已经交付完整帧，接收端的精确重复规则保证结果仍然正确。

当前协议没有分片级端到端 ACK。若发送端写入成功后 ctrl_server 在完成下游转发前进程退出，发送端无法判定该分片是否落到接收队列；本轮传输最终会因缺少区间或超时失败，调用方需要重新发起整条命令。弱网断点续传需要版本化会话、分片 ACK 和持久化清单，不能用盲目重放写命令替代。

## 6. 资源与时限边界

| 项目 | 当前限制 |
| --- | ---: |
| 控制帧 | 1 MiB |
| 数据帧 | 64 MiB + 64 KiB 协议余量 |
| 小文件内存路径 | 32 MiB |
| 大文件分片 | 4 MiB |
| 单文件最大值 | 1 GiB |
| 单文件最大分片数 | 4096 |
| 发送在途分片 | 3 |
| real_ctrl 目标数据连接 | 3，至少 1 条可降级运行 |
| ctrl_kik 目标数据连接 | 3 |
| 服务端单 Kik 数据连接上限 | 4 |
| 服务端 CtrlData 连接上限 | 8 |
| 数据接收队列 | 2 帧 |
| 队列持续拥塞 | 30 秒后失败 |
| 单次等待数据 | 6 分钟 |
| 整体大文件传输 | 4 小时 |
| 服务端/被控端长命令 | 4 小时 5 分钟 |
| real_ctrl 长响应等待 | 4 小时 10 分钟 |

容量为 2 的接收队列约持有 8 MiB 文件内容。发送端最坏同时保留 3 组 Dok 缓冲和外层网络帧，连续编码阶段约为 24 MiB；这是固定窗口约束下的可预测上限，不会随文件大小增长。ctrl_server 的并发接收与转发也受实际数据连接数、每连接单读任务和下游写背压约束。

## 7. 连续帧编码的取舍

当前实现有意使用容易审计的连续编码：

1. 源分片编码为连续 `Dok::FilePart`。
2. Dok 缓冲再编码为连续 CtrlData/KikData 网络帧。
3. ctrl_server 完整解析 ID 和 payload，再编码目标协议帧。

这会产生受 4 MiB 分片与 3 帧窗口限制的内存复制，但带来三个生产收益：完整帧可以不依赖借用生命周期在其他连接重试；发送代码只有一种路径；服务端不会依赖两套协议“布局恰好相同”进行原地改写。相比向量写和手工拼接多层切片，这里的复制开销不是当前链路的主风险，磁盘读取、SHA-256、TLS/Noise、网络和持久化 fsync 更可能成为瓶颈。

`Channel` 仍不套 `BufWriter`：当前每个协议帧都会立即 flush，额外用户态缓冲不能形成批量收益，反而增加小控制帧复制。

## 8. 磁盘与 CPU 成本

- 元数据必须在控制命令/响应中携带 SHA-256，所以发送前必须先完成一遍摘要读取，再读取一遍用于发送；两遍读取复用同一个打开句柄，第二遍通常命中系统文件缓存。
- 接收端允许乱序，不能用到达顺序直接累计最终 SHA-256；完成后重新顺序读取临时文件是端到端完整性边界。
- 多路分片会增加 seek。临时文件已预分配，3 帧小窗口限制随机性，Windows 文件缓存会吸收大部分小范围定位成本；最终摘要读取仍是顺序 I/O。
- `sync_all` 和最终 SHA-256 会增加完成延迟，但分别保证持久化和内容完整性，生产路径不应为表面吞吐删除。
- 递归目录统计的同步 WalkDir/rayon 已放入 `spawn_blocking`，不会占住 Tokio 核心工作线程。

## 9. 后续演进边界

- 断点续传：需要版本号、传输会话 ID、分片 ACK/位图、临时文件恢复规则和最终提交协议。
- 多文件并行：需要服务端支持多个活动命令，并按命令 ID 建独立响应及数据路由；不能直接放开现有单命令信号量。
- 自适应窗口：应基于 RTT、吞吐、磁盘队列和内存预算测量后调整，不能无限增加数据连接或在途帧。
- Windows `TransmitFile` 或 kTLS 无法直接覆盖当前自定义多层帧、rustls 和双向中继，现阶段复杂度高于收益。

## 10. 生产监控建议

- 监控数据连接实际建立数、换路次数、写超时、队列拥塞、传输总超时、重复分片计数和 SHA-256 失败。
- 监控系统临时目录中的孤立 `.temp` 数量；正常失败路径会清理，进程被强制终止可能遗留文件，可按进程部署策略定期清理过期项。
- 用户要求临时文件固定进入系统临时目录，因此目标位于其他卷时无法同时保证原子替换。当前以 SHA-256 后 copy+fsync+remove 保证内容完整性；若业务必须跨卷原子切换，需要把临时目录部署到目标卷。
- 大量并发文件任务应由上层调度多个独立会话；单会话保持一个活动命令，确保响应和数据关联确定。
- 三个组件必须同步升级；当前 v3 线协议不保留旧帧、明文或数据面元数据兼容分支。

## 11. 公网性能结论（2026-09-12）

- 最终 32 MiB 完整验收：应用上传 0.40 MiB/s、下载 0.39 MiB/s，多连接乱序分片双向 SHA-256 一致。
- 同轮 8 MiB SFTP 基线：上行 3.67 MiB/s、下行 0.40 MiB/s。中继任一方向都要经过
  公网较慢的下行，应用达到该瓶颈的 97.5%，没有观察到 CPU、Noise 或状态机限速；此前
  多次 8 MiB 应用复验为上传约 0.41 MiB/s、下载 0.39–0.40 MiB/s，结论一致。
- 最终验收活动连接和文件状态下，服务端为 60,524 KiB RSS、5 个线程；历史空闲采样约
  10 MiB RSS。内存增量符合固定 4 MiB 分片、3 帧窗口、双向中继缓冲和运行时缓存的有界设计。
- 3 个在途 4 MiB 分片形成约 12 MiB 窗口，已远大于当前带宽时延积。生产上保持现值；
  只有在高带宽高 RTT 环境以对照基线证明窗口不足后，才引入有上限的自适应窗口。
