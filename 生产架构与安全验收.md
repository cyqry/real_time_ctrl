# real_time_ctrl 生产架构与安全验收

## 1. 验收结论

| 目标 | 结论 | 真实边界 |
| --- | --- | --- |
| 数据传输加密、控制端防中间人 | 已完成 | `real_ctrl` 使用 TLS 1.3 + 证书链/服务名 + SPKI pin；`ctrl_kik` 使用 Noise NK，只持有原始 X25519 服务端公钥并验证持钥，不接收证书、服务名或机器信息 |
| 生产加固、提高逆向成本 | 已完成可执行基线 | `ctrl_kik` 采用固定工具链、重编译标准库、全项目字符串密文、fat LTO、strip、panic abort、CFG/CET；代码签名不属于防逆向门禁，任何客户端仍应假设可被逆向 |
| `real_ctrl` 开放 API | 已完成 | HTTP `POST /api/v1/commands` 与 `GET /api/screen`；新版命名管道使用同一 `ApiRequest` / `ApiResponse` 契约 |
| 多账号、多实例、命令并行 | 已完成 | 账号 ACL；同账号多实例与多账号并存；HTTP 请求和服务端命令按关联 ID 独立并行，CLI 保持串行 |
| 扩展性与维护性 | 已完成本轮收敛 | 三个 real_ctrl bin 共用 library 组合根；连接、协议、API、策略分层；四级并发配额、有界队列、超时、会话隔离和稳定错误结构 |

本结论适用于当前“单 ctrl_server、多账号、每账号多 real_ctrl 实例、多个 ctrl_kik”的拓扑。
`screen_stream` 和 `start` 未改动；`ctrl_kik/src/screen` 只迁移发布态字符串，捕获行为不变。

生产监听按职责拆分：灰度为 Kik Noise 9005 / real_ctrl TLS 9009，正式为
Kik Noise 9002 / real_ctrl TLS 9007。相同端口配置会启动失败，旧分流协议已经删除。

## 2. 运行架构

```text
HTTP / Windows named pipe / CLI
              |
              v
 real_ctrl::api_service::RealCtrlApi
              |
              v
 dispatch -> Context::request -> Agent
              |
      pinned TLS 1.3
              |
              v
 ctrl_server TLS management listener
      | Ctrl / CtrlData session
      v
 selected Kik -> bounded command/data channel
              |
 Noise NK authenticated encryption
              |
              v
           ctrl_kik
```

`real_ctrl/src/lib.rs` 是业务组合根。`real_ctrl`、`real_ctrl_local_server`、`real_ctrl_invoker_http_service` 只负责各自进程入口，不再重复声明业务模块。

## 3. 强身份控制面执行路径

1. `real_ctrl` 优先从 `REAL_CTRL_AUTH_SECRET` 读取至少 32 字符的部署秘密；未提供时使用
   发布构建写入的加密默认值。
2. `connect_real_ctrl` 连接 `REAL_CTRL_TLS_PORT`，TCP 连接和 TLS 握手均受超时控制。
3. rustls 只启用 TLS 1.3，先校验证书链和 `REAL_CTRL_TLS_SERVER_NAME`，再校验叶子证书 SubjectPublicKeyInfo 的 SHA-256 pin。
4. TLS 内发送 `CtrlAuthStart(account_id, instance_id, client_nonce)`。
5. `ctrl_server` 返回随机 `CtrlAuthChallenge(server_nonce)`。
6. 客户端用所属账号秘密对账号、实例和双 nonce 计算 HMAC-SHA256 proof；服务端常量时间验证后签发随机 `session_id`。
7. 每条 `CtrlData` 连接重新完成 pinned TLS，并用 `session_id + channel_nonce + HMAC proof` 绑定控制会话。
8. 服务端拒绝重复 nonce、过期会话、非当前控制连接、超过 8 条的数据通道；同账号同实例重连
   只替换自身旧会话，不影响其他实例。
9. 控制会话没有当前目标时，服务端自动选择最近上线且账号 ACL 允许的在线 Kik；Kik 后上线、
   控制端后上线及双方并发上线均覆盖。自动选择不覆盖 `sys_use`，当前目标完整下线时自动回退到
   仍在线的合法候选。

服务端端口角色策略：

- TLS 管理端口只允许 `Ctrl` / `CtrlData`。
- Kik Noise 端口只允许 `Kik` / `KikData`。
- 明文、静态摘要认证和同端口兼容路径已经删除。

## 4. ctrl_kik 安全边界

`ctrl_kik` 只知道服务端 IP/端口、固定原始 X25519 公钥和自身 kik id，不读取证书、pin、服务名或服务端机器信息。由此得到的严格结论是：

- 固定公钥用于验证 Noise 对端持钥，链路可阻断主动中间人读取；它不是账号、PKI 或机器身份声明。
- kik id 是重连标识，不是安全凭据。
- 真实控制授权必须由 TLS 管理面和 ctrl_server 策略承担。
- 若删除或动态接受该公钥，链路只能防被动抓包，不能再宣称抵抗主动中间人。

## 5. 资源与协议护栏

| 项目 | 当前限制 |
| --- | --- |
| 握手帧 | 4 KiB |
| 控制帧 | 1 MiB |
| 数据帧 | 64 MiB + 64 KiB 协议余量 |
| 活动服务端连接 | 512 |
| CtrlData / session | 8 |
| KikData / kik | 4 |
| pipe 请求 / 响应 | 1 MiB / 64 MiB |
| pipe 并发 | 16 |
| HTTP JSON body | 1 MiB |
| API 二进制响应 | 48 MiB |
| 控制会话 TTL | 12 小时 |
| session nonce 记录 | 1024 |
| HTTP/API 并行命令 | 32 / real_ctrl 进程 |
| 服务端命令并发 | 全局 256；账号默认 64；实例默认 16；Kik 16 |
| 服务端数据路由 | 全局 4096；每会话 64；最长 4 小时 15 分钟 |

所有 TCP/TLS 读、写、握手和 pipe I/O 都有超时。数据转发不再逐帧创建无界任务，而是等待下游写入形成背压。

大文件统一使用 4 MiB 分片，单文件最多 1 GiB；数据帧不再保留 1 GiB 整文件兼容余量。生产部署仍需通过网络入口限流、服务端连接上限和文件大小策略共同约束。

## 6. 开放 API

HTTP 默认配置：

```text
127.0.0.1:9000
GET  /api/health
POST /api/v1/commands
GET  /api/screen
```

HTTP 行为：

- 非 loopback 绑定时，启动阶段强制校验至少 32 字符的 `REAL_CTRL_API_TOKEN`。
- token 支持 `Authorization: Bearer` 或 `X-Real-Ctrl-Token`，比较使用常量时间逻辑。
- 请求体上限 1 MiB，全局请求超时 370 秒，panic 转换为 HTTP 错误。
- 内部 anyhow 详情只写本地日志，对外返回稳定错误码和通用消息。
- `Exec` 默认像其他命令一样允许；仍经过 real_ctrl 与 ctrl_server 两层可覆盖策略。
- 路由由 HTTP 启动组合根显式安装，不依赖跨 crate 的隐式 inventory 链接副作用。
- HTTP 与管道共享 32 个有界许可，每个请求有独立命令关联 ID、oneshot 响应和数据队列；请求可并行执行、乱序返回，达到上限才返回稳定 `busy`。
- 交互控制台输入循环仍然逐条等待响应。网络断开只做单飞重连，已写出的命令不会自动重放。
- `sys_history` 可不带 ID 查询最近 256 条有界历史，也可按 Kik ID 精确查询；只返回当前 ctrl_server 进程实际观察到的最近上线/下线时间，不做虚假持久化承诺。

Windows 命名管道行为：

- 管道名：`\\.\pipe\real_ctrl_service_pipe`。
- `accept_remote(false)`、`inheritable(false)`。
- DACL 只允许 System、Administrators 和对象 Owner。
- 新协议：`RTCAPI1\0 + JSON ApiRequest/ApiResponse`。
- 无 magic 的旧 postcard 请求直接拒绝，不保留双协议解析。
- HTTP 与 pipe 都进入 `RealCtrlApi`，不能绕过策略层。

## 7. Exec 管理面授权

任意 shell 是高危能力，必须同时满足：

1. API 进程设置 `REAL_CTRL_API_ALLOW_EXEC=1`。
2. 服务端设置 `CTRL_SERVER_ALLOW_EXEC=1`。

`ctrl_kik` 默认包含并执行 Exec，不再使用 Cargo feature 门禁。任一管理面授权缺失都会拒绝；执行输出会被排空并限制为 stdout/stderr 各保留 1 MiB，任务超时取消时子进程随 future 终止。

## 8. 反逆向与构建

反逆向不是可证明的安全属性。当前措施的目标是提高成本并确保逆向后也拿不到全局控制秘密：

- `REAL_CTRL_AUTH_SECRET` / `CTRL_SERVER_AUTH_SECRET` 可在运行时覆盖发布构建的加密默认值。
- build.rs 统一提供端口、绑定地址、TLS 服务名和锁路径等非敏感默认值；运行时环境变量可覆盖。
- 构建输入由 `build.rs` 校验后交给 `hidden!(env!(...))` 在编译期转成密文数组；发布脚本同时搜索
  原始秘密、账号 JSON Base64 和其中每个账号 secret，任一明文命中即失败。
- 编译期字符串生成到 Cargo `OUT_DIR`，不再由 build.rs 修改源码。
- 字符串混淆使用每字段唯一 GCM nonce，但密钥仍在二进制内，因此只算混淆，不算秘密存储。
- `hardened` 启用 opt-level 3、strip、fat LTO、单 codegen unit、panic abort、overflow checks。
- `real_ctrl` / `ctrl_server` 由 `scripts/build_hardened.ps1` 使用继承该基线的 `production` profile
  构建，但覆盖为保留 PDB/DWARF，继续支持部署诊断与性能观测。
- `ctrl_kik` 由 `scripts/build_ctrl_kik_protected.ps1` 独立构建：固定完整 rustc commit，重新编译标准库，使用 immediate-abort、位置移除、路径重映射、fat LTO、符号剥离、CFG、CET、ASLR 和 NX。
- protected 构建关闭默认 feature，`ctrl_kik` 自身日志宏在编译期展开为空；二进制审计要求 `.rs`、源码/Cargo/编译器绝对路径、PDB、panic 位置、应用模块名与日志框架痕迹全部为零。
- Rust 标准库和 Tokio 仍可能保留少量公开运行时字符串；这些字符串不包含项目源码或秘密，纳入计数回归，不通过魔改上游依赖制造表面上的“语言不可识别”。
- Authenticode 只能证明发布者与签名后的文件完整性，不能阻止反汇编、调试或内存提取；当前 `ctrl_kik` 不再把代码签名作为防逆向或生产发布门禁。

`real_ctrl` / `ctrl_server` 生产构建：

```powershell
.\scripts\build_hardened.ps1
```

`ctrl_kik` 正式构建与验收：

```powershell
.\scripts\build_ctrl_kik_protected.ps1
```

详细参数生成、门禁和剩余边界见 `ctrl_kik/防逆向发布.md`。

## 9. 验收命令

```powershell
cargo test -p common -p ctrl_common -p real_ctrl -p ctrl_server -p ctrl_kik
cargo check -p common -p ctrl_common -p real_ctrl -p ctrl_server -p ctrl_kik
cargo clippy -p common -p ctrl_common -p real_ctrl -p ctrl_server -p ctrl_kik --all-targets
.\scripts\audit_production_dependencies.ps1
.\scripts\e2e_ctrl_stack.ps1
```

E2E 必须覆盖：

- 正确 pin 的 TLS 管理连接成功。
- 错误 SPKI pin 被拒绝。
- Kik Noise 端口拒绝 real_ctrl TLS 角色。
- Noise Kik/KikData v4 接入成功。
- 同账号两个控制实例、不同账号控制实例同时在线且选择状态互不干扰。
- 同一 HTTP 实例至少 6 个有耗时命令真实并行，响应按各自关联 ID 正确返回。
- HTTP 缺少 token 返回 401。
- `sys_list -> sys_history -> sys_use -> sys_now -> ctrl_ls` 完整跨组件链路成功，并在真实 Kik 进程退出后观察到最近下线时间。
- 大文件经过 `ctrl_set_big_file -> ctrl_get_big_file` 双向分片传输后，源文件、远端文件和下载文件 SHA-256 一致。

## 10. 生产部署门槛

- 使用企业 PKI 或受控私有 CA 签发服务端证书，私钥 ACL 只允许服务账号。
- 控制面 HMAC secret 与 HTTP token 独立生成、独立轮换，只保存在仓库 `target` 的 ACL 保护
  身份目录及必要进程的混淆默认值中，不写入源码、manifest 或命令行历史。
- 固定并审计 SPKI pin 更新流程；证书换钥时必须走显式 pin 轮换。
- 外网只开放必要端口，并在操作系统/边界防火墙补充源地址、连接速率和审计策略。
- 发布侧保存并校验 production 服务产物的 SHA-256 和 protected Kik 的 `build-receipt.json`；
  这些哈希用于发布追踪，不提供不可逆向能力。
- `ctrl_kik` 的 `target/ctrl-kik-protected/build-receipt.json` 必须满足 `production_ready=true`。
- `ctrl_kik` 必须固定部署生成的原始 X25519 公钥并使用 Noise NK；接受任意公钥或自动降级明文都不能满足主动中间人不可读取的要求。

## 11. 当前灰度验收（2026-09-13）

- 旧明文、静态摘要认证、旧初始化帧和 postcard 管道代码已删除；Kik Noise 与 real_ctrl TLS
  强制拆分端口，v4 灰度为 9005/9009。
- 本地 E2E 验证同账号多实例、多账号多实例和 6 条真实并行 HTTP 命令；最后一轮并行耗时
  2.173 秒，旧串行链路至少需要约 12 秒。
- protected `ctrl_kik.exe` 的 receipt 为 `production_ready=true`，全项目发布态明文和
  源码/调试痕迹审计通过；大小与 SHA-256 以当次发布 manifest 为准。
- 最终公网 E2E 覆盖错误 pin、角色隔离、HTTP token、在线/离线历史、Exec、小文件和
  32 MiB 乱序分片双向 SHA-256，全部通过；上传/下载均约 0.40 MiB/s，下行基线 0.42 MiB/s，
  中继效率 95.2%。远端服务哈希与 manifest 一致且 systemd 为 active。
- `ctrl_kik/src/screen` 发布态运行文本已经纳入 `hidden!` 与全项目明文门禁；测试专用文本不进入 PE。
