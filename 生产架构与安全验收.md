# real_time_ctrl 生产架构与安全验收

## 1. 验收结论

| 目标 | 结论 | 真实边界 |
| --- | --- | --- |
| 数据传输加密、控制端防中间人 | `real_ctrl -> ctrl_server` 已完成 | TLS 1.3 + 证书链/服务名校验 + 叶子证书 SPKI SHA-256 pin；`ctrl_kik` 默认明文且不校验服务端，因此不宣称抗 MITM |
| 生产加固、提高逆向成本 | 已完成可执行基线 | 控制秘密不再编译进二进制；hardened profile、fat LTO、strip、panic abort、整数溢出检查、CFG 和可选代码签名；任何客户端仍应假设可被逆向 |
| `real_ctrl` 开放 API | 已完成 | HTTP `POST /api/v1/commands` 与 `GET /api/screen`；新版命名管道使用同一 `ApiRequest` / `ApiResponse` 契约 |
| 扩展性与维护性 | 已完成本轮收敛 | 三个 real_ctrl bin 共用 library 组合根；连接、协议、API、策略分层；有界队列、超时、会话隔离和稳定错误结构 |

本结论适用于当前“单 ctrl_server、单活动 real_ctrl、多个 ctrl_kik”的单租户拓扑。`screen_stream` 和 `start` 未改动。

## 2. 运行架构

```text
HTTP / Windows named pipe / CLI
              |
              v
 real_ctrl::api_service::RealCtrlApi
              |
              v
 dispatch -> Context::request -> Agent
              |
      pinned TLS 1.3
              |
              v
 ctrl_server TLS management listener
      | Ctrl / CtrlData session
      v
 selected Kik -> bounded command/data channel
              |
       plain TCP by design
              |
              v
           ctrl_kik
```

`real_ctrl/src/lib.rs` 是业务组合根。`real_ctrl`、`real_ctrl_local_server`、`real_ctrl_invoker_http_service` 只负责各自进程入口，不再重复声明业务模块。

## 3. 强身份控制面执行路径

1. `real_ctrl` 从 `REAL_CTRL_AUTH_SECRET` 读取至少 32 字符的部署秘密。
2. `connect_real_ctrl` 连接 `REAL_CTRL_TLS_PORT`，TCP 连接和 TLS 握手均受超时控制。
3. rustls 只启用 TLS 1.3，先校验证书链和 `REAL_CTRL_TLS_SERVER_NAME`，再校验叶子证书 SubjectPublicKeyInfo 的 SHA-256 pin。
4. TLS 内发送 `CtrlAuthStart(client_nonce)`。
5. `ctrl_server` 返回随机 `CtrlAuthChallenge(server_nonce)`。
6. 客户端用部署秘密计算 HMAC-SHA256 proof；服务端常量时间验证后签发随机 `session_id`。
7. 每条 `CtrlData` 连接重新完成 pinned TLS，并用 `session_id + channel_nonce + HMAC proof` 绑定控制会话。
8. 服务端拒绝重复 nonce、过期会话、非当前控制连接、超过 8 条的数据通道。

服务端端口角色策略：

- TLS 管理端口只允许 `Ctrl` / `CtrlData`。
- 明文端口默认只允许 `Kik` / `KikData`。
- 明文控制迁移需要服务端 `CTRL_SERVER_ALLOW_PLAIN_CTRL=1` 和客户端 `REAL_CTRL_ALLOW_PLAIN=1` 同时显式开启；生产禁止开启。

## 4. ctrl_kik 安全边界

`ctrl_kik` 仍只知道服务端 IP/端口和自身 kik id，不读取证书、pin、服务名或服务端机器信息。由此得到的严格结论是：

- 默认链路不能抵抗主动中间人。
- kik id 是重连标识，不是安全凭据。
- 真实控制授权必须由 TLS 管理面和 ctrl_server 策略承担。
- 如果未来要求 ctrl_kik 链路抗 MITM，必须新增显式高安全变体并为它配置可验证的服务端身份；不能用“机会型加密”冒充身份认证。

## 5. 资源与协议护栏

| 项目 | 当前限制 |
| --- | --- |
| 握手帧 | 4 KiB |
| 控制帧 | 1 MiB |
| 历史数据帧 | 1 GiB + 16 MiB 兼容余量 |
| 活动服务端连接 | 512 |
| CtrlData / session | 8 |
| KikData / kik | 4 |
| pipe 请求 / 响应 | 1 MiB / 64 MiB |
| pipe 并发 | 16 |
| HTTP JSON body | 1 MiB |
| API 二进制响应 | 48 MiB |
| 控制会话 TTL | 12 小时 |
| session nonce 记录 | 1024 |

所有 TCP/TLS 读、写、握手和 pipe I/O 都有超时。数据转发不再逐帧创建无界任务，而是等待下游写入形成背压。

历史数据协议仍允许大帧，这是兼容现有文件/截图传输的明确维护债务；不能在未引入流式下载契约前直接缩小全局数据上限。生产部署应通过网络入口限流、服务端连接上限和文件大小策略共同约束。

## 6. 开放 API

HTTP 默认配置：

```text
127.0.0.1:9000
GET  /api/health
POST /api/v1/commands
GET  /api/screen
```

HTTP 行为：

- 非 loopback 绑定时，启动阶段强制校验至少 32 字符的 `REAL_CTRL_API_TOKEN`。
- token 支持 `Authorization: Bearer` 或 `X-Real-Ctrl-Token`，比较使用常量时间逻辑。
- 请求体上限 1 MiB，全局请求超时 370 秒，panic 转换为 HTTP 错误。
- 内部 anyhow 详情只写本地日志，对外返回稳定错误码和通用消息。
- `Exec` 默认拒绝。
- 路由由 HTTP 启动组合根显式安装，不依赖跨 crate 的隐式 inventory 链接副作用。

Windows 命名管道行为：

- 管道名：`\\.\pipe\real_ctrl_service_pipe`。
- `accept_remote(false)`、`inheritable(false)`。
- DACL 只允许 System、Administrators 和对象 Owner。
- 新协议：`RTCAPI1\0 + JSON ApiRequest/ApiResponse`。
- 旧 postcard `InputCommand` 仅保留兼容。
- HTTP 与 pipe 都进入 `RealCtrlApi`，不能绕过策略层。

## 7. Exec 三层授权

任意 shell 是高危能力，必须同时满足：

1. API 进程设置 `REAL_CTRL_API_ALLOW_EXEC=1`。
2. 服务端设置 `CTRL_SERVER_ALLOW_EXEC=1`。
3. `ctrl_kik` 使用 `--features dangerous-exec` 构建。

任一层缺失都会拒绝。即使启用，输出也会被排空并限制为 stdout/stderr 各保留 1 MiB，任务超时取消时子进程随 future 终止。

## 8. 反逆向与构建

反逆向不是可证明的安全属性。当前措施的目标是提高成本并确保逆向后也拿不到全局控制秘密：

- `REAL_CTRL_AUTH_SECRET` / `CTRL_SERVER_AUTH_SECRET` 只在部署时注入。
- 编译期字符串生成到 Cargo `OUT_DIR`，不再由 build.rs 修改源码。
- 字符串混淆使用每字段唯一 GCM nonce，但密钥仍在二进制内，因此只算混淆，不算秘密存储。
- `hardened` 启用 opt-level 3、strip、fat LTO、单 codegen unit、panic abort、overflow checks。
- `scripts/build_hardened.ps1` 额外启用 Windows Control Flow Guard，可选调用 `signtool` 做 SHA-256 Authenticode 签名和时间戳。

生产构建：

```powershell
.\scripts\build_hardened.ps1
```

带签名构建：

```powershell
.\scripts\build_hardened.ps1 -SignCertificateThumbprint "<CERT_THUMBPRINT>"
```

## 9. 验收命令

```powershell
cargo test -p common -p ctrl_common -p real_ctrl -p ctrl_server -p ctrl_kik
cargo check -p common -p ctrl_common -p real_ctrl -p ctrl_server -p ctrl_kik
cargo clippy -p common -p ctrl_common -p real_ctrl -p ctrl_server -p ctrl_kik --all-targets
cargo build -p ctrl_server -p ctrl_kik -p real_ctrl
.\scripts\e2e_ctrl_stack.ps1
```

E2E 必须覆盖：

- 正确 pin 的 TLS 管理连接成功。
- 错误 SPKI pin 被拒绝。
- 明文端口默认拒绝 real_ctrl。
- HTTP 缺少 token 返回 401。
- `sys_list -> sys_use -> sys_now -> ctrl_ls` 完整跨三实例链路成功。

## 10. 生产部署门槛

- 使用企业 PKI 或受控私有 CA 签发服务端证书，私钥 ACL 只允许服务账号。
- 控制面 HMAC secret 与 HTTP token 独立生成、独立轮换，不写入仓库或命令行历史。
- 固定并审计 SPKI pin 更新流程；证书换钥时必须走显式 pin 轮换。
- 外网只开放必要端口，并在操作系统/边界防火墙补充源地址、连接速率和审计策略。
- 对 hardened 产物做 Authenticode 签名，并在发布侧验证签名和哈希。
- 不得把 ctrl_kik 默认链路描述为加密或抗 MITM。

## 11. 本轮实际验收结果（2026-07-11）

- 五个核心 crate 的 `cargo check` 与 `cargo clippy --all-targets` 通过；仅保留冻结目录 `ctrl_kik/src/screen` 的 7 条既有备用接口告警。
- 核心测试共 73 个通过；1 个依赖 Windows 交互桌面的截屏对比测试按原设计忽略。
- 三实例 E2E 全部通过，包含错误 pin、禁止明文降级、HTTP token 拒绝和完整命令转发负向/正向路径。
- `scripts/build_hardened.ps1` 使用 `--locked` 成功生成 5 个启用 CFG 的 hardened 可执行文件。
- 本机验收未提供代码签名证书，因此产物尚未做 Authenticode 签名；签名仍是正式发布门槛。
