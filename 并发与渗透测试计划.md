# 并发与渗透测试计划

## 1. 目标与授权边界

本计划验证 `real_ctrl -> ctrl_server -> ctrl_kik` 在生产约束下的并发隔离、抗畸形输入、认证授权、
传输机密性、资源上限和故障恢复能力。测试只作用于本仓库启动的本地实例，以及明确授权的
`ytycc.com` 灰度端口（Kik Noise `9005`、控制 TLS `9009`）。正式端口 `9002/9007` 不进行压力、
破坏性或凭据猜测测试。

测试遵循 OWASP Web Security Testing Guide、OWASP ASVS 和 NIST SP 800-115 的通用方法，但按本项目
威胁模型裁剪：不扫描第三方地址、不进行持久化、不暴力破解账号秘密、不突破操作系统权限边界，
不以拒绝服务为目标。公网测试采用有界并发并在结束后清理测试进程和文件。

## 2. 关键安全假设

- 控制面必须使用 TLS 1.3、证书链和 SPKI pin 双重验证；错误 pin、错误端口角色和错误账号秘密必须失败。
- Kik 面使用 Noise NK。它验证服务端持有固定 X25519 私钥并加密链路，但客户端是匿名的；不能把 Kik
  的连接本身当成账号身份认证。
- HTTP 只默认监听回环地址；命令接口要求高熵 token。命名管道依赖 Windows DACL，只允许 SYSTEM、
  Administrators 和当前进程所有者。
- 同账号不同控制实例、不同账号以及同一 HTTP 进程中的每次请求都必须独立路由；任何跨请求响应、
  数据关联 ID 或当前 Kik 泄漏均为阻断发布的问题。
- 控制帧、数据帧、HTTP body、路径、命令、会话、连接、并发任务和数据路由都必须存在硬上限。

## 3. 测试矩阵

| 类别 | 核心用例 | 通过标准 |
|---|---|---|
| 构建与静态门禁 | `cargo test`、`cargo clippy`、依赖审计、protected Kik 二进制审计 | 零测试失败；生产活动依赖无未豁免漏洞；Kik receipt 为 production ready |
| HTTP 鉴权 | 缺失 token、错误 token、错误认证方案、合法 Bearer、合法专用 header、无 token 的畸形 JSON | 未授权请求在读取/解析命令 body 前 100% 返回 401；合法请求不受影响；错误不泄漏 token |
| HTTP 输入边界 | 畸形 JSON、未知命令、未知字段、错误版本、超长 request ID、NUL、超过 1 MiB body、错误方法 | 返回确定的 4xx/结构化错误；超限 body 也可在继续读取前直接断开；无 5xx、panic、进程退出或后续健康检查失败 |
| 浏览器边界 | 非允许 Origin 的 CORS 预检 | 不返回攻击者 Origin 的允许头；真实命令仍必须经过 token |
| TLS/Noise | TLS 1.2、错误 SPKI、TLS 接 Noise 端口、明文/随机/截断载荷、520 条 Kik 慢连接饱和 | TLS 1.2 和角色混淆均失败；Kik 配额耗尽不能占用 TLS 管理面许可；服务端保持健康 |
| 会话隔离 | 同账号多实例、多账号多实例、同实例并发重连 | 只替换同账号同 instance；其他会话不掉线、不继承其当前 Kik 或响应 |
| 命令并发 | 6 个 HTTP 耗时命令、12 个命名管道客户端、48 个突发命令、100 个批次请求 | 确认并行而非串行；超过许可的请求快速返回 `busy` 类错误；无超时和关联错配 |
| 文件并发 | 多个独立小文件并行下载、12 MiB 多分片双向传输、命令与文件交叠 | 每个目标 SHA-256 一致；乱序路由不串流；临时文件最终清理 |
| 配额与恢复 | instance/account/Kik/global 命令许可、每会话数据路由、数据 nonce 重放 | 第 N+1 个请求被拒绝；释放后立即恢复；一个租户的配额不阻塞另一个租户 |
| 解析器鲁棒性 | 固定种子的随机短帧、声明长度不匹配、空帧、尾随字节 | 所有解析入口只返回失败或合法值，不 panic、不无界分配 |
| 下线与清理 | Kik 强制退出、控制会话退出、失败传输 | 最近下线时间可查询；路由、连接、许可和 `.temp` 文件没有泄漏 |

## 4. 压力模型与阈值

本地全栈测试使用 48 个同时到达的约 3 秒命令，故意超过单实例 16 和本地 API 32 的许可。预期至少
有一批命令并行成功，其余请求在外层超时前明确拒绝；不允许排队形成数分钟尾延迟。稳定性循环至少
执行 100 次轻量命令，并检查每个 `request_id` 原样返回。

文件测试以多个独立 256 KiB 文件和一个 12 MiB 文件覆盖单帧、多分片、路由并发和 SHA-256 完整性。
该规模用于持续回归而非带宽极限测量。公网灰度压力默认更保守，避免影响共享主机；吞吐结果必须同时
记录应用传输和独立链路基线，不能仅凭单次公网结果认定协议瓶颈。

## 5. 缺陷分级与发布门禁

- **P0**：未授权命令执行、链路明文、跨账号/实例数据泄漏、任意内存破坏。立即停止发布。
- **P1**：可重复崩溃、配额绕过、文件损坏、身份校验降级、关联 ID 串线。修复并完整回归后方可发布。
- **P2**：有界资源放大、错误分类不准确、恢复时间超标、日志注入。应在当前版本修复。
- **P3**：诊断、文档或低风险加固问题。记录责任人和后续期限。

测试通过要求：P0/P1/P2 未关闭数量为零；所有强制自动化用例通过；失败后健康探针、合法命令和新文件
传输均能恢复；本地报告、日志、证书和测试数据只出现在 `target/e2e` 或 `target/security-tests`。

## 6. 剩余风险与人工验证

Kik 在 Noise NK 中是匿名发起方，这是当前明确的协议边界：公网攻击者可以尝试注册伪 Kik，但不能读取
其他已建立加密连接。生产账号应使用精确 `allowed_kiks`，不应使用 `"*"`；Kik ID 具有足够随机性但
不是密码学客户端凭据，因此一旦 ID 外泄仍存在冒用风险。若未来要求服务端证明 Kik 身份，必须新增独立
的设备注册/轮换机制，不能把固定全局秘密塞进所有 `ctrl_kik`。

Kik 与 TLS 已使用独立连接硬上限，所以匿名 Kik 饱和不能直接饿死管理端口；但没有客户端身份凭据时，
服务端无法区分真实与伪造 Kik，同一 Kik 端口本身的可用性仍依赖上游防火墙/四层限速和主机连接监控。

命名管道的跨 Windows 用户 DACL 验证、长时间 soak、断网/丢包/限速注入、杀进程时磁盘写满，以及
EDR/杀毒兼容性依赖专用环境，不能由单一开发机完全证明。这些项目应在预发布 Windows 虚拟机和独立
灰度主机上周期执行，并保留资源曲线和系统事件日志。
