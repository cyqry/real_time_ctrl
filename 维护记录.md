# real_time_ctrl 维护记录

本文件只记录当前生产版本的重要决策；历史阶段计划已经删除。强制维护规则以各级
`AGENTS.md` 为准，完整架构见 `生产架构与安全验收.md`。

## 2026-09：v4 多账号并发协议

- `real_ctrl -> ctrl_server` 使用 pinned TLS 1.3，校验证书链、DNS 名称和 SPKI pin。
- `ctrl_kik -> ctrl_server` 的命令与三条数据连接统一使用 Noise NK
  25519/ChaChaPoly/BLAKE2s；客户端只持有服务端原始公钥。
- 灰度端口为 Noise 9005 / TLS 9009，正式端口为 Noise 9002 / TLS 9007；相同端口启动失败。
- 旧明文、静态摘要认证、同端口分流、旧初始化帧和 postcard 管道协议均已删除。
- 控制认证 proof 绑定 `account_id + instance_id + client_nonce + server_nonce`；同账号不同实例、
  不同账号可并存，同账号同实例重连只替换自身旧会话。
- 控制秘密、Noise 私钥、TLS 私钥和 API token 从仓库 `target` 下受 ACL 保护的身份文件进入
  加密构建默认值，发布 EXE 可直接运行；同名运行环境变量优先，便于应急轮换。

## 文件传输

- 元数据随控制命令/响应传输；数据通道只传 4 MiB `FilePart` 或结构化错误。
- 两端各使用三条数据连接和最多三个在途分片。分片可乱序到达，完全重复幂等，部分重叠、
  越界和长度不符立即拒绝，最终以 SHA-256 校验完整性。
- 接收文件在系统临时目录以 128-bit CSPRNG 随机名和 `.temp` 后缀创建，预分配后按偏移写入，
  flush、fsync、摘要通过才提交。同卷原子移动；跨卷为 copy+fsync+remove。
- 最终 32 MiB 公网上传、下载均为 0.40 MiB/s，同轮 SFTP 下行基线 0.42 MiB/s，转发效率
  95.2%。当前瓶颈是公网下行，不扩大连接数或固定窗口。
- 三段独立连接均允许有界预到达：服务端 CtrlData 最多 32 帧等待 5 秒；Kik 和 real_ctrl
  分别最多 16 条、合计 64 MiB、TTL 30 秒，避免跨 TCP 流乱序被误判或迟到帧永久占槽。
- 下载业务失败不携带 data ID；服务端保留 Kik 的真实读取错误并释放预登记路由，只有成功响应
  回显错误 ID 或错误成功类型时才报告协议关联错误。`getfile` 上限为 32 MiB，更大文件使用
  `getbigfile` 的 4 MiB 分片链路。

## 控制端与状态

- CLI、HTTP 和命名管道共用 `RealCtrlApi` 与稳定 JSON 契约。CLI 保持串行；HTTP/管道使用最多
  32 个有界许可、per-command oneshot 和 per-data-id 队列并行执行。
- 服务端使用全局 256、账号默认 64、实例默认 16、Kik 16 的四级并发限制；客户端命令 ID
  不进入 Kik，服务端内部随机 ID 将响应定向回原账号实例。
- HTTP 默认只监听 loopback；非 loopback 必须提供至少 32 字符 token。命名管道限制本机主体。
- `Exec` 在 Kik 默认编译，管理面必须同时设置 `REAL_CTRL_API_ALLOW_EXEC=1` 和
  `CTRL_SERVER_ALLOW_EXEC=1`。
- `sys_history [kik_id]` 返回当前服务端进程观察到的最近上线/下线时间，最多保留 256 条，
  不承诺跨服务端重启持久化。
- 控制会话未选择目标时自动使用最近上线且 ACL 允许的在线 Kik；Kik 与控制端无论谁先连接都能
  收敛到同一状态。显式 `sys_use` 优先，当前目标完整下线后自动选择仍在线的合法候选。

## 构建与发布

- `ctrl_kik` 使用固定 Rust 提交、重编译标准库、无日志构建和 PE/字符串审计；真实结果以
  `target/ctrl-kik-protected/build-receipt.json` 为准。
- `real_ctrl` 和 `ctrl_server` 使用 production profile，保留 PDB/DWARF 与运行日志。
- `scripts/publish_stack.ps1` 负责身份复用、依赖审计、三端构建、远端原子替换和公网 E2E。
- 地址、端口、TLS PEM、pin、认证秘密、API token 和策略由 `build.rs` 接受构建变量并生成
  混淆默认值，运行环境可覆盖。发布门禁会搜索原始值，发现静态明文即失败。
- 发布包只包含可直接运行的二进制、调试符号和审计回执；每次构建先清空旧 artifact 目录，
  不再生成运行启动器或复制配置、证书和秘密文件。

## 当前验收基线

- `scripts/test_security_concurrency.ps1` 统一执行运输 crate 测试、严格 Clippy、生产依赖 RustSec
  门禁和本地全栈安全并发 E2E，机器可读摘要统一写入仓库 `target/security-tests/`。
- 本地 E2E 覆盖 520 条 Kik 慢连接隔离、HTTP/命名管道与多账号多实例并发、48 请求过载、
  100 请求恢复、协议畸形输入、6 路文件并发和 12 MiB 多连接乱序分片 SHA-256。
- 公网 E2E 覆盖错误 pin、TLS 1.2、端口角色隔离、HTTP 鉴权前置、Kik 上下线、Exec、24 请求
  配额突发、大小文件和 32 MiB 多连接乱序分片 SHA-256；发布后服务保持 active 且无 warning。
- TLS 与匿名 Kik 使用独立连接配额，避免 Kik 慢握手挤占管理面；公网层面的容量型攻击仍应由
  防火墙、四层限速和主机监控共同处理。
- 初始化帧和本地 API 请求的日志只记录类型、长度与结果，不记录 proof、nonce、session ID、
  Exec 命令、路径或完整请求体；远端可批量触发的拒绝事件降为 debug，避免日志放大。
- `start`、`screen_stream` 保持未修改；`ctrl_kik/src/screen` 的发布态运行文本已经纳入
  `hidden!` 与全项目明文门禁，捕获行为保持不变。
