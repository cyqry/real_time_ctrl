# real_time_ctrl 维护记录

本文件只记录当前生产版本的重要决策；历史阶段计划已经删除。强制维护规则以各级
`AGENTS.md` 为准，完整架构见 `生产架构与安全验收.md`。

## 2026-09：v3 安全协议

- `real_ctrl -> ctrl_server` 使用 pinned TLS 1.3，校验证书链、DNS 名称和 SPKI pin。
- `ctrl_kik -> ctrl_server` 的命令与三条数据连接统一使用 Noise NK
  25519/ChaChaPoly/BLAKE2s；客户端只持有服务端原始公钥。
- 灰度端口为 Noise 9005 / TLS 9009，正式端口为 Noise 9002 / TLS 9007；相同端口启动失败。
- 旧明文、静态摘要认证、同端口分流、旧初始化帧和 postcard 管道协议均已删除。
- 控制秘密、Noise 私钥、TLS 私钥和 API token 从仓库 `target` 下受 ACL 保护的身份文件进入
  加密构建默认值，发布 EXE 可直接运行；同名运行环境变量优先，便于应急轮换。

## 文件传输

- 元数据随控制命令/响应传输；数据通道只传 4 MiB `FilePart` 或结构化错误。
- 两端各使用三条数据连接和最多三个在途分片。分片可乱序到达，完全重复幂等，部分重叠、
  越界和长度不符立即拒绝，最终以 SHA-256 校验完整性。
- 接收文件在系统临时目录以 128-bit CSPRNG 随机名和 `.temp` 后缀创建，预分配后按偏移写入，
  flush、fsync、摘要通过才提交。同卷原子移动；跨卷为 copy+fsync+remove。
- 最终 32 MiB 公网上传 0.40 MiB/s、下载 0.39 MiB/s，同轮 SFTP 下行基线 0.40 MiB/s，
  转发效率 97.5%。当前瓶颈是公网下行，不扩大连接数或固定窗口。

## 控制端与状态

- CLI、HTTP 和命名管道共用 `RealCtrlApi`、稳定 JSON 契约和单命令门禁。
- HTTP 默认只监听 loopback；非 loopback 必须提供至少 32 字符 token。命名管道限制本机主体。
- `Exec` 在 Kik 默认编译，管理面必须同时设置 `REAL_CTRL_API_ALLOW_EXEC=1` 和
  `CTRL_SERVER_ALLOW_EXEC=1`。
- `sys_history [kik_id]` 返回当前服务端进程观察到的最近上线/下线时间，最多保留 256 条，
  不承诺跨服务端重启持久化。

## 构建与发布

- `ctrl_kik` 使用固定 Rust 提交、重编译标准库、无日志构建和 PE/字符串审计；真实结果以
  `target/ctrl-kik-protected/build-receipt.json` 为准。
- `real_ctrl` 和 `ctrl_server` 使用 production profile，保留 PDB/DWARF 与运行日志。
- `scripts/publish_stack.ps1` 负责身份复用、依赖审计、三端构建、远端原子替换和公网 E2E。
- 地址、端口、TLS PEM、pin、认证秘密、API token 和策略由 `build.rs` 接受构建变量并生成
  混淆默认值，运行环境可覆盖。发布门禁会搜索原始值，发现静态明文即失败。
- 发布包只包含可直接运行的二进制、调试符号和审计回执；每次构建先清空旧 artifact 目录，
  不再生成运行启动器或复制配置、证书和秘密文件。

## 当前验收基线

- Rust 单元测试、`cargo check`、clippy、定向 rustfmt、生产依赖 RustSec 门禁通过。
- 公网 E2E 覆盖错误 pin、端口角色隔离、HTTP token、Kik 上下线、Exec、大小文件和 32 MiB
  多连接乱序分片 SHA-256。
- `start`、`screen_stream` 保持未修改；`ctrl_kik/src/screen` 的发布态运行文本已经纳入
  `hidden!` 与全项目明文门禁，捕获行为保持不变。
