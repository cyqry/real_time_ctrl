# real_time_ctrl 维护记录

## 2026-07-08

### 当前约束

- Android 控制端暂不实现。
- `screen_stream` 暂不改动。
- `real_ctrl` 需要校验 `ctrl_server` 身份，防中间人攻击。
- `ctrl_kik` 保持极小客户端，不做账号式身份校验，不获取除服务端 IP/端口外的服务端机器信息。

### 已确认事实

- 工作区包含 `real_ctrl`、`ctrl_server`、`ctrl_kik`、`common`、`ctrl_common`、`screen_stream` 等 crate。
- 当前远程连接使用 `TcpStream` + `LengthFieldBasedFrameDecoder` + 自定义二进制帧。
- 当前 `real_ctrl` 的历史认证是用户名/密码静态摘要，不足以作为抗中间人的安全边界。
- `real_ctrl` 已有命名管道和 HTTP API 雏形，但还未完全收敛为稳定开放 API。

### 已完成

- 新增根级和核心 crate 级 `AGENTS.md`，明确后续 AI 维护边界。
- 新增本维护记录，持续记录进度、坑和设计取舍。
- `common::ltc_codec::LengthFieldBasedFrameDecoder` 增加可配置最大帧长度，默认上限保守兼容旧大文件路径。
- 修复 `Frame`、`KikFrame`、`Resp`、`ServerResp` 对空帧/短帧可能 panic 的解析路径，改为返回 `None`。
- 为长度帧、控制数据帧、Kik 数据帧和短响应帧补充畸形输入测试。
- 客户端侧已按通道角色接入帧上限：`real_ctrl` / `ctrl_kik` 的命令通道使用控制上限，数据通道使用数据上限。
- `ctrl_server` 在读取 `InitFrame` 前尚不知道连接角色，当前显式使用数据通道上限保持兼容。
- 新增 `real_ctrl::api_service::RealCtrlApi` 作为本地开放 API 的统一服务层雏形，命名管道服务已改为通过该入口执行命令。
- 本地命名管道增加请求/响应大小上限：请求 1 MiB，响应 64 MiB；客户端和服务端都会校验，避免按恶意长度直接分配内存。
- 已运行 `cargo test -p common -p ctrl_common`，测试通过。
- 已运行 `cargo check -p real_ctrl -p ctrl_kik -p ctrl_server`，检查通过。

### 遇到的坑

- 仓库里不少中文注释在当前 PowerShell 输出中显示为乱码，后续不要因为显示问题误删原注释。
- 现有 `GetBigFile` 路径可能一次性传输较大的数据帧；在重构为流式分片前，不能把全局帧限制直接压到 1 MiB。
- `ctrl_kik` 不校验服务端身份是明确设计约束；不要在后续文档里误写成默认抗 MITM。
- 直接运行 `cargo fmt` 会格式化整个 workspace，造成大量无关 diff。本轮已恢复这些格式化噪声；后续应优先对目标文件做局部格式化或在提交前认真检查 `git diff --stat`。
- 单文件运行 `rustfmt` 时需要传 `--edition 2021`，否则 async 文件会按 Rust 2015 解析并报错。

### 下一步

- 梳理 `real_ctrl` 本地 pipe / HTTP API 的统一服务层边界。
- 设计 `real_ctrl -> ctrl_server` 的服务端公钥 pinning 配置结构。
- 后续服务端需要在完成 `InitFrame` 后按 `ChannelType` 进一步收紧控制连接上限。
- 后续 HTTP 路由应逐步复用 `RealCtrlApi`，不要继续在 handler 中直接散落命令拼装逻辑。
- 命名管道后续还需要补 Windows ACL/SDDL 限制，当前只完成了消息大小护栏。

### 阶段一收束记录

- 阶段一“架构基线与协议护栏”已完成一版可验收基线，详见根目录 `阶段一_协议与执行路径基线.md`。
- 新增 `common::message::protocol_hello::ProtocolHello`，用于后续协议版本、连接角色和能力协商；当前只作为预留协议结构，不接入真实连接流程，避免破坏老链路。
- `ctrl_server` 现在会在首帧前使用数据通道上限保持兼容，识别 `ChannelType` 后再把 `Ctrl` / `Kik` 收紧到控制通道上限。
- `real_ctrl` 本地 pipe 服务已走 `RealCtrlApi` 统一入口，并在客户端和服务端同时限制请求/响应大小。
- 已确认 `screen_stream` 没有 diff。
- 验证通过：`cargo test -p common -p ctrl_common`。
- 验证通过：`cargo check -p real_ctrl -p ctrl_kik -p ctrl_server`。

### 新增维护注意

- 阶段一只是护栏，不是传输加密；不要把当前状态描述成已经抗 MITM。
- `ProtocolHello` 尚未参与线上握手；后续接入时要设计兼容窗口，并确保 `real_ctrl` 强安全模式不能降级到明文。
- 服务端帧上限“先宽后紧”是为了兼容首帧识别角色；如果未来把首帧独立为固定小握手，可以把服务端初始上限降到控制级。

### 阶段二收束记录

- 阶段二“real_ctrl 强身份传输基线”已完成一版，详见根目录 `阶段二_real_ctrl强身份传输基线.md`。
- 新增 `common::secure_transport`，统一封装 `real_ctrl` pinned TLS 客户端、`ctrl_server` TLS acceptor、证书 SPKI pin 计算和连接拆分。
- `real_ctrl` 默认走 pinned TLS；只有显式设置 `REAL_CTRL_ALLOW_PLAIN=1` 才允许旧明文兼容模式。
- `ctrl_server` 配置 `CTRL_SERVER_TLS_CERT` 和 `CTRL_SERVER_TLS_KEY` 后会额外监听 TLS 管理端口，默认端口 `9443`。
- TLS 管理端口限制为 TLS 1.3。
- `common::channel::Channel` 的 writer 已改为异步写 trait object，以便明文 TCP 和 TLS 流复用同一套业务写路径。
- `ctrl_kik` 仍保持明文最小链路，不读取服务端证书、服务名、pin 或其他服务端机器信息。

### 阶段二踩坑

- `tokio-rustls` TLS 流拆分后的 writer 类型不再是 `TcpStream::OwnedWriteHalf`，所以不能继续把 `Channel` 写死到 TCP 写半边。
- `x509-parser` 的 `X509Certificate::from_der` 需要显式导入 `FromDer` trait。
- SPKI pin 不是整张证书 DER 的 hash；后续工具和文档必须写清楚是 `subject_public_key_info_der`。
- 强安全模式下缺少 `REAL_CTRL_TLS_CA_CERT` 或 `REAL_CTRL_TLS_SERVER_SPKI_SHA256` 必须失败，不能悄悄回退明文。

## 阶段四：开放 API 与本地入口加固

- 新增 `real_ctrl/src/api_contract.rs`，HTTP 和新版命名管道共用 `ApiRequest` / `ApiResponse`。
- `real_ctrl_invoker_http_service` 现在只创建一份 `Context`，再同时交给 HTTP `RealCtrlApi` 组件和本地管道服务，避免两套 real_ctrl 连接。
- HTTP 新增 `POST /api/v1/commands`，保留 `GET /api/screen`；两者都走 `RealCtrlApi`。
- `config/app.toml` 默认绑定改为 `127.0.0.1`，CORS 从 `*` 收紧到 localhost。
- `REAL_CTRL_API_TOKEN` 配置后，HTTP 必须带 `Authorization: Bearer <token>` 或 `X-Real-Ctrl-Token`。
- 开放 API 默认禁用 `Exec`，必须显式设置 `REAL_CTRL_API_ALLOW_EXEC=1` 才允许。
- 新版命名管道协议使用 `RTCAPI1\0` magic + JSON；旧协议继续使用 postcard `InputCommand`。
- 坑：不要把新版 pipe API 改回 postcard。`ApiCommand` 使用 `#[serde(tag = "kind")]`，postcard 不支持该形态。
- 命名管道创建时加入 SDDL：`D:P(A;;GA;;;SY)(A;;GA;;;BA)(A;;GA;;;OW)`，并显式禁用远程 pipe 和继承句柄。
- 根 `Cargo.toml` 新增 `hardened` profile，用于生产构建符号剥离、fat LTO、单 codegen unit 和 panic abort。

### 阶段三收束记录

- 阶段三“real_ctrl 会话与数据通道绑定”已完成一版，详见根目录 `阶段三_real_ctrl会话与数据通道绑定.md`。
- 新增 `common::session_auth`，使用 OS 随机数生成 nonce，并用 HMAC-SHA256 生成/校验 proof。
- `InitFrame` 新增 v2 控制通道 challenge/session 帧，以及数据通道 session 绑定帧。
- `real_ctrl` 默认 TLS 模式不再发送历史静态摘要；明文兼容模式才发送旧 `CtrlAuthReq` / `CtrlDataConnReq`。
- `ctrl_server` 新增 `CtrlSession` 状态，数据通道必须绑定当前 session，且 `channel_nonce` 在同一 session 内不能重复使用。
- `ctrl_kik` 协议仍未改变。

### 阶段三踩坑

- 新增 v2 帧时不能改变旧 `InitFrame` 字符串帧编码；旧帧必须继续是 `code + raw string`，否则会破坏 `ctrl_kik` 和兼容链路。
- v2 新帧可以使用长度前缀，但解析必须拒绝短帧和尾随字节。
- TLS 只解决服务端身份和传输加密；业务层仍需要 session 绑定，否则数据通道仍可能错绑或重放。

### 最终验收补充

- 已修复两个历史环境耦合测试：`ctrl_kik/src/main.rs` 不再读取固定 `E:\D\`，`start/src/main.rs` 不再读取开发机 `target\release` 下的固定 exe。
- 上述测试数据现在都生成在仓库 `target` 目录下，符合“除 Cargo/全局依赖外，生成文件可控在项目内”的约束。
- 已运行 `cargo test --workspace`，全工作区测试通过。
- 已运行 `cargo check --workspace`、`cargo check -p real_ctrl --profile hardened`，检查通过。
- 已确认 `git diff -- screen_stream` 为空，`screen_stream` 未被改动。
- `git diff --check` 仅报告 Windows 换行转换提示，无空白错误。

### 最终验收踩坑

- workspace 级测试会暴露旧测试里写死的开发机私有路径。后续新增测试必须自建数据，不能依赖本机盘符、用户目录或已有构建产物。
- PowerShell 仍可能把 UTF-8 中文文件名/注释显示成乱码；确认内容时使用 `Get-Content -Encoding UTF8`，不要靠默认输出判断文件损坏。

### 三实例端到端验收记录

- 新增 `scripts/e2e_ctrl_stack.ps1`，用于启动 `ctrl_server`、`ctrl_kik`、`real_ctrl_invoker_http_service` 三个实例并通过 HTTP API 验收链路。
- E2E 默认端口：明文 `9002`、TLS `19443`、HTTP `9000`；证书、日志、trace、报告和测试 marker 都生成在仓库 `target/e2e` 下。`ctrl_kik` 已恢复使用编译期 `LOCK_FILE_PATH()`，测试运行时会沿用原始锁路径。
- E2E 生成自签证书并计算 SPKI SHA-256 pin，`real_ctrl` 通过 `REAL_CTRL_TLS_CA_CERT`、`REAL_CTRL_TLS_SERVER_SPKI_SHA256` 和 `REAL_CTRL_TLS_SERVER_NAME` 校验服务端身份。
- E2E 已通过：`health`、未带 token 返回 `401`、`sys_list` 发现 `ctrl_kik`、`sys_use` 选择实例、`sys_now` 查询当前控制目标、`ctrl_ls` 经 `ctrl_server -> ctrl_kik` 执行成功。
- 最终报告：`target/e2e/e2e_report.json`，最后一次结果 `success=true`，完成时间 `2026-07-08T20:50:01.0758259+08:00`。
- 为支持本地 E2E 和受控部署，`ctrl_server` 保留 `CTRL_SERVER_BIND_HOST`、`CTRL_SERVER_PORT`，`real_ctrl` 使用 `REAL_CTRL_SERVER_HOST`、`REAL_CTRL_SERVER_PORT`、`REAL_CTRL_HTTP_LOCK_PATH`；`ctrl_kik` 不再提供锁路径、服务端地址或端口环境变量覆盖。

### 三实例端到端踩坑

- `ctrl_server` 原读循环写法 `match timeout(... framed_arc.lock().await.next()).await` 会让 reader 锁的临时值活到 match 结束；match 臂内再次锁同一个 `framed_arc` 调整 decoder 上限时会自锁。现已改成先在局部块读取，再进入 match。
- 同类读循环已同步收敛到 `ctrl_server`、`real_ctrl`、`ctrl_kik` 的控制/数据连接，后续不要把 `lock().await.next()` 直接塞进 match 条件。
- E2E 初版用裸 TCP 探测 TLS 端口，会在 `ctrl_server` 错误日志里制造 `tls handshake eof`。现已改为 `openssl s_client` 完整 TLS 握手探测。
- Windows PowerShell 在 `$ErrorActionPreference=Stop` 下会把 OpenSSL 证书生成进度 stderr 当成 `NativeCommandError`；脚本里 OpenSSL 调用需要临时降级为 `Continue` 并把输出写入 `target/e2e/logs`。
- `netstat` 粗匹配端口会把 `TIME_WAIT` 的远端端口误判为本地占用；脚本已改为优先使用 `Get-NetTCPConnection -LocalPort <port> -State Listen`。

### 生产环境变量与 ctrl_kik 配置收敛

- 按最新约束，`ctrl_kik` 的锁路径、服务端地址和端口全部恢复为编译期配置：`LOCK_FILE_PATH()`、`get_host()`、`PORT()`，不再提供环境变量覆盖。
- `real_ctrl` 的服务端连接配置改为专属环境变量：`REAL_CTRL_SERVER_HOST`、`REAL_CTRL_SERVER_PORT`，避免误影响 `ctrl_kik`。
- 已在根目录生成 `生产环境变量_IDEA.txt`，内容为 IDEA 可直接粘贴的分号分隔环境变量。
- 已在仓库内生成本机部署证书：`target/prod/certs/server.crt`、`target/prod/certs/server.key`，SPKI pin 为 `913de71e8a1817ffc42a4e00f753f8b0fc2918fda596aed6c1a11b61cc1510b5`。

## 2026-07-10：ctrl_kik 无损截屏修复与优化

- 现场确认 `ctrl_kik/src/screen.rs` 原 DXGI 测试的首帧为 2560x1440 全零资源；同一 duplication 会话第 1 帧起正常，黑屏不在 PNG 编码层。
- 保留原 `cut_screen()` 函数体，新增 `cut_screen_dxgi(PngProfile)`；`CtrlCommand::Screen` 生产默认改为 DXGI `Balanced`。
- DXGI 首帧全零时只重取一次，仍全黑时由旧 GDI 路径复核，避免把合法纯黑桌面直接判错。
- 新入口使用容量 1 的有界异步队列和专用 Windows 线程，持续复用 D3D11 device、duplication、staging texture 与 RGB 缓冲区。
- 截屏请求加入 5 秒排队超时和 30 秒结果超时；捕获失败会完整重建会话一次，稳定阶段帧等待收紧到 100 ms。
- PNG 提供 `Fast`、`Balanced`、`High` 三档，全部为 RGB8 逐像素无损；对比测试会用同一原始帧编码并逐字节校验解码结果。
- 新增 `ctrl_kik/src/screen/AGENTS.md` 和根目录 `截屏实现与验收.md`，记录执行路径、性能数据、旧 GDI 风险和维护边界。
- 测试产物统一写入 `target/screen`；依赖交互桌面的对比测试标记为 `ignore`。

### 截屏踩坑

- Desktop Duplication 的首个资源在部分驱动上可能是全零初始化帧，不能把首次 `AcquireNextFrame` 成功等同于已有有效桌面内容。
- 旧 GDI 24bpp DIB 行跨度需要 4 字节对齐；原函数按 `width * 3` 处理，只能保留作兼容对照，生产默认不要切回。
- 旧 GDI 路径可能受进程 DPI awareness 影响返回逻辑分辨率；现场曾出现 2048x1152，而 DXGI 返回物理 2560x1440。
- `PrimaryScreenCapturer` 使用 single-threaded D3D11 device，禁止跨 Tokio 工作线程移动；重复截图应由专用线程持有会话。
- High 压缩通常只比 Balanced 小少量，但 CPU 成本显著更高；远程命令默认 Balanced 更合理。

### 2026-07-11 截屏开闭原则重构

- 新增 `screen/backend.rs`，用 `ScreenCaptureBackend`、`CaptureRequest`、`CaptureArtifact` 和 `ScreenCaptureService` 固化调度边界。
- DXGI worker 移入 `screen/dxgi_backend.rs`，旧 GDI 通过 `screen/gdi_backend.rs` 适配；两个后端不再互相调用。
- `CaptureArtifact` 使用 `Accept` / `VerifyWithFallback` 表达结果语义，是否降级由管线统一决定。
- `cmd_runner` 改为只依赖 `capture_screen(CaptureRequest)`；新增后端无需修改命令执行流程。
- 默认组合根仍按 DXGI、GDI 顺序注册；DXGI 报错或连续全黑时由管线自动尝试 GDI。
- 外层两轮会话尝试已收紧：只有 `ACCESS_LOST`、`DEVICE_REMOVED`、`DEVICE_RESET` 类瞬时错误才完整重建一次，确定性错误立即交给后续后端。
- 新增 5 个无桌面管线测试，覆盖成功短路、待复核降级、错误降级、错误聚合和空配置拒绝。
- 全 crate 测试发现既有 `context::test` 故意在释放 `String` 后继续解引用，nightly UB 检查会中止进程；该问题与截屏改造无关，截屏测试使用 `cargo test -p ctrl_kik screen::` 独立验收。

### 2026-07-11 截屏目录化与会话过期

- 原 `ctrl_kik/src/screen.rs` 已移动为 `ctrl_kik/src/screen/mod.rs`，截屏实现全部收敛到 `ctrl_kik/src/screen/`，外部 `screen::*` 调用路径不变。
- 原低层 `screen/lib.rs` 改名为 `screen/desktop_duplication.rs`，避免与 crate 根 `lib.rs` 语义混淆；历史 GDI 实现和交互对比测试分别移动到 `legacy_gdi.rs`、`tests.rs`。
- DXGI Session 新增 30 秒空闲过期：最后一次请求完成后若无新请求，capturer 在所属标准库工作线程内析构；下一次请求再惰性初始化。
- 工作线程及容量为 1 的请求队列继续保留，但过期后不持有 D3D11/DXGI 对象、staging texture、RGB 缓冲或桌面帧，兼顾线程所有权、低空闲占用和突发请求性能。
- 会话超时接收逻辑使用工作线程内部的 current-thread Tokio runtime；不得将 capturer 迁移到应用 Tokio 线程，也不得从其他线程直接释放 COM 对象。
- `cargo test -p ctrl_kik screen:: -- --nocapture` 通过：17 个通过、1 个交互测试忽略；其中包含会话过期和无会话惰性等待测试。
- `cargo test -p ctrl_kik --release screen::tests::compare_capture_backends -- --ignored --nocapture` 通过；DXGI Balanced 输出 2560x1440、390648 B，人工查看画面完整且非黑屏，三档 PNG 解码像素一致。
- `cargo check -p ctrl_kik --profile hardened` 通过；`cargo test -p ctrl_kik -- --skip context::test` 通过 19 个、忽略 1 个。未执行已知会触发释放后解引用的 `context::test`。
### 2026-07-11 生产安全与可维护性收敛

- 控制面凭据从编译期 `USER_NAME/PASSWORD` 迁移为 `REAL_CTRL_AUTH_SECRET` / `CTRL_SERVER_AUTH_SECRET` 部署注入；common/config.json 不再包含控制秘密。
- ctrl_server 增加端口角色策略：TLS 只允许 Ctrl/CtrlData，明文默认只允许 Kik/KikData；E2E 新增错误 SPKI pin 与明文控制拒绝断言。
- TLS 握手、TCP 连接、Channel 写入/关闭、pipe I/O 全部加入超时；握手帧限制为 4 KiB，活动连接上限 512。
- 修复旧 Ctrl inactive 清除新会话竞态、控制断线自动重放非幂等命令、服务端逐数据帧无界 spawn、Kik 上线先于响应队列发布等问题。
- 删除 ctrl_kik 数据等待中的裸指针/释放后解引用实现和 UB 测试，改用容量 8 的安全有界通道；全量 ctrl_kik 测试不再需要跳过 context::test。
- ctrl_kik Exec 曾使用 `dangerous-exec` feature 门禁，随后按产品要求恢复为默认编译；服务端仍保留独立最终授权开关。
- 大文件接收先写目标同目录临时文件，SHA-256 校验后使用 `MoveFileExW` 原子替换，失败不提前破坏目标文件。
- real_ctrl 三个 bin 收敛到 `src/lib.rs`，移除重复模块声明、示例 HTTP 模型和无实现的 async_util。
- HTTP/pipe 共用 API 契约，增加字段长度、并发、body、二进制响应、非 loopback token 和稳定错误边界。
- hardened profile 改为 opt-level 3 + overflow checks；新增 `scripts/build_hardened.ps1` 启用 CFG 和可选 Authenticode 签名。
- 普通 `cargo check` 的非 screen 冻结模块警告已清理；clippy 剩余提示仅来自本轮明确不改的 `ctrl_kik/src/screen` 兼容导出/备用接口。
- E2E 进程输出改为启动后立即异步排空，避免错误 pin 的 backtrace 填满 stderr 后将安全拒绝误报为超时；PowerShell 脚本使用带 BOM 的 UTF-8 以兼容 Windows PowerShell 5.1。
- real_ctrl library 中的 HTTP 路由改为由启动组合根显式安装，避免 inventory 对象因无直接符号引用而未链接，三实例 E2E 已覆盖真实路由表。

### 2026-07-11 第二轮生产代码质量收敛

- `Channel` 移除 `undefined_id` 哨兵和未初始化 ID panic，连接扩展属性改用 `ChannelAttributeKey<T>` 约束键与类型。
- 线协议 `Command` 移除本地生命周期变体，`InputCtrlCommand -> CtrlCommand` 改为可失败转换，消除生产路径 `unreachable!`。
- ctrl_server 使用单个 `CtrlState` 原子维护控制连接、数据连接和 TLS 会话，并新增旧连接清理/当前连接清理回归测试。
- ctrl_server 已认证读处理器拆分为 control、data、kik、init 四个子模块，在线表锁不再跨网络关闭等待。
- real_ctrl API 增加 Context 级单命令门禁与稳定 busy 响应；HTTP token 使用 SHA-256 固定长度摘要和 subtle 常量时间比较。
- ctrl_kik 初始化通道移除 Any downcast；大文件分片新增越界、长度、重复和重叠校验，失败会清理临时文件，错误不再返回成功响应。
- hardened profile 默认日志级别修正为 INFO；显式 RUST_LOG 优先于编译期默认值。
- 核心测试 79 个通过、1 个交互桌面测试忽略，三实例 E2E 成功。

### 2026-07-11 构建默认值与 Exec 策略调整

- 非敏感启动默认值统一由各 crate 的 build.rs 注入：服务端绑定地址/端口、real_ctrl 服务端口、HTTP 锁路径、TLS 端口和服务名。
- 运行时同名环境变量仍可覆盖编译默认值，便于同一产物在不同端口部署。
- 控制面 secret、服务端私钥和 SPKI pin 明确禁止使用 cargo:rustc-env，避免进入可执行文件。
- ctrl_kik 移除 dangerous-exec feature，Exec 默认与其他命令进入同一执行分派；API 和 ctrl_server 两层授权保持不变。

## 生产身份材料自动生成

- 新增 `scripts/generate_production_identity.ps1`，在仓库 `target/prod` 内生成控制面随机秘密、独立 HTTP API token、TLS 证书/私钥和 SPKI SHA-256 pin。
- 同时生成按进程拆分的 PowerShell 环境脚本与 IDEA 分号配置；敏感文件通过 Windows ACL 限制为当前用户和 LocalSystem 可访问。
- 默认拒绝覆盖已有身份，只有显式 `-Force` 才执行轮换，避免无意破坏已经部署的证书 pin 和会话认证秘密。
